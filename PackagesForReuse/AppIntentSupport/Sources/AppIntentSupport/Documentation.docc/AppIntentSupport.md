# ``AppIntentSupport``

Reusable helpers for host apps that expose actions through App Intents.

## Overview

`AppIntentSupport` intentionally stays small. Host apps define concrete intents and shortcut providers; this package only provides product-neutral validation mechanics and a marker for future App Intents package composition.
