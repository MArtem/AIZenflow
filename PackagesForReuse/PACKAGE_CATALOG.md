# Package Catalog

## Purpose

This catalog is the high-level selector for the reusable package library. Read this file first when deciding which package to adopt, then open the package-specific `README.md` for setup, boundaries and verification.

Use the short summary for quick scanning. Use **Expanded Description** to understand what the package actually owns and whether it fits the feature. The package-specific `README.md` remains the implementation and setup source of truth.

## Counts

- **Reusable root packages**: 40
- **Integration helper packages**: 5
- **Currently active in `./PackagesInUse`**: 21 root packages and 4 helper folders

## Root Packages

| Package | Active In TchopApp | Products | Short Summary | Expanded Description | Host/App Keeps |
| --- | --- | --- | --- | --- | --- |
| `./PackagesForReuse/AppAnalytics` | Yes | `AppAnalyticsCore, AppAnalytics` | Analytics event modeling/reporting for app features and package integrations. | Use when features or infrastructure need structured analytics events instead of ad-hoc strings. It defines event identity, typed attributes, reporter contracts, and optional app-facing analytics surfaces so navigation/networking/push helpers can emit consistent telemetry. | Host apps own the event taxonomy, sampling/export policy, user/session metadata classification, privacy review, and the decision of which product actions are worth instrumenting. |
| `./PackagesForReuse/AppAppleAuthentication` | Yes | `AppAppleAuthentication` | Sign in with Apple request, credential and authorization helpers. | Use when an app needs reusable Apple authentication mechanics: authorization requests, credential/result modeling, nonce/state helpers, cancellation/error handling, and testable boundaries around Apple auth flows. | Host apps own account linking, backend token exchange, session persistence, onboarding copy, legal/privacy flows, and user-facing auth errors. |
| `./PackagesForReuse/AppBackgroundTasks` | No | `AppBackgroundTasks` | Reusable background task scheduling/execution boundary. | Use when a host app needs a product-neutral wrapper around background task registration, task identifiers, execution attempts, expiration handling, and testable scheduling decisions without putting BGTaskScheduler details in features. | Host apps own concrete background task identifiers, Info.plist capabilities, work payloads, retry policy, battery/network constraints, and production monitoring. |
| `./PackagesForReuse/AppBranding` | Yes | `AppBranding` | Brand identity primitives and semantic branding values. | Use when multiple targets need the same product-neutral representation of app identity, brand names, visual roles, icons, or target-driven presentation choices without scattering literals across UI code. | Host apps own actual brand assets, trademark/product copy, design-system token mapping, and release-specific branding decisions. |
| `./PackagesForReuse/AppCache` | No | `AppCache` | Generic cache storage, expiration and cleanup mechanics. | Use when data can be regenerated and needs bounded local caching with expiration, eviction, corrupted-entry handling, and deterministic cache tests separated from feature code. | Host apps own cache keys, privacy classification, encryption/file-protection requirements, invalidation triggers, and whether stale cached content may be shown. |
| `./PackagesForReuse/AppConfiguration` | Yes | `AppConfiguration` | Runtime/static configuration snapshot and manager mechanics. | Use when app settings or UI/runtime configuration need a typed snapshot, provider/store boundary, refresh flow, and deterministic fallback behavior without coupling configuration mechanics to a specific screen. | Host apps own the configuration schema, remote fetch source, rollout policy, default values, stale-data rules, and user-visible behavior when refresh fails. |
| `./PackagesForReuse/AppConnectivity` | No | `AppConnectivity` | Network connectivity state monitoring and policy helpers. | Use when features need a reusable connectivity snapshot, constrained/expensive network flags, transition observation, or testable manual monitors for offline-aware components. | Host apps own offline UI, retry/sync behavior, telemetry, copy, and decisions about whether work is blocked or allowed on constrained networks. |
| `./PackagesForReuse/AppDatabase` | Yes | `AppDatabaseCore, AppSwiftDataDatabase, AppCoreDataDatabase, AppDatabaseComposition, AppDatabase` | Database abstractions/composition plus SwiftData/Core Data execution boundaries. | Use when persistence code needs explicit database operation contracts, SwiftData/Core Data managers, model-actor/background boundaries, migration primitives, and composition helpers that keep storage mechanisms reusable. | Host apps own schemas, model registration, migrations for product data, seed/bootstrap policy, repository queries, data-loss policy, and UI-specific fetch/update strategy. |
| `./PackagesForReuse/AppDeviceInfo` | No | `AppDeviceInfo` | Device, OS and runtime information provider utilities. | Use when code needs testable access to device model, OS version, idiom, locale/runtime capability, or environment facts without directly reading global platform APIs in business logic. | Host apps own feature gating policy, analytics use of device metadata, privacy review, compatibility matrix, and user-visible unsupported-device behavior. |
| `./PackagesForReuse/AppDownloads` | No | `AppDownloads` | Secure generic download service with destination-file policies. | Use when the product needs standalone download mechanics: HTTPS-by-default URL validation, size limits, cancellation preservation, replacement/fail-if-exists file policies, temporary work-file cleanup, and deterministic tests. | Host apps own what is downloaded, authentication, checksum/domain validation, user progress UI, storage location, retry copy, and post-download import semantics. |
| `./PackagesForReuse/AppEnvironment` | Yes | `AppEnvironment` | Environment/runtime provider helpers for app mode and build context. | Use when code needs a typed environment snapshot for development/staging/production, UI-test mode, build/runtime flags, or dependency selection without spreading environment conditionals across the app. | Host apps own concrete environment names, secrets/API keys, base URLs, entitlement-dependent behavior, and production safety rules. |
| `./PackagesForReuse/AppErrors` | Yes | `AppErrorsCore, AppErrors` | Generic app error models, mapping, reporting and user-facing boundaries. | Use when features need consistent error severity, supportability, presentation/reporting contracts, redaction, and mapping from technical failures into app-level error surfaces. | Host apps own localized copy, support links, escalation policy, product-specific recovery actions, analytics export, and whether failures are blocking or recoverable. |
| `./PackagesForReuse/AppFeatureFlags` | No | `AppFeatureFlags` | Feature flag definitions, evaluation and storage mechanics. | Use when a product needs typed feature flags, local overrides, persisted snapshots, percentage rollouts, variant bucketing, and validation before enabling runtime behavior. | Host apps own flag names, rollout owners, cleanup dates, remote config fetch, exposure analytics, kill-switch policy, and user-facing behavior. |
| `./PackagesForReuse/AppFileStorage` | Yes | `AppFileStorage` | Safe local file storage domains and copy/write helpers. | Use when app or extension code needs predictable local file domains, sanitized file names, safe writes/copies, app-group/local container boundaries, and testable storage operations outside SwiftUI render paths. | Host apps own file retention policy, media semantics, privacy/file-protection level, user deletion behavior, and migration of existing file references. |
| `./PackagesForReuse/AppFormValidation` | Yes | `AppFormValidation` | Form snapshot validation and form state mechanics. | Use when a feature has form fields, validation rules, async load/save snapshots, revision handling, and a controller boundary that keeps validation state out of SwiftUI views. | Host apps own localized validation messages, product-specific rules, field labels, submit throttling, backend validation, and UX presentation. |
| `./PackagesForReuse/AppGlassUI` | Yes | `AppGlassUI` | Reusable SwiftUI glass/Liquid Glass-style UI primitives. | Use when SwiftUI screens need reusable availability checks, shape-based glass styling, fallback paths, or shared chrome primitives while keeping visual implementation consistent across targets. | Host apps own semantic colors, exact product visuals, layout, interaction behavior, accessibility labels, and Figma/design-system decisions. |
| `./PackagesForReuse/AppImagePipeline` | No | `AppImagePipeline` | Image loading, decoding, caching and prefetch pipeline. | Use when images come from remote/provided bytes and need bounded disk/memory caching, content-type allowlists, decoding/downsampling, prefetch cancellation, and privacy-safe diagnostics. | Host apps own image sources, auth, placeholders, product cache policy, transformations, accessibility text, and whether a local media pipeline is more appropriate. |
| `./PackagesForReuse/AppInputFormatting` | No | `AppInputFormatting` | Reusable user-input formatting with cursor mapping and formatter pipelines. | Use when text fields need repeatable formatting such as digit filtering, grouping, max length, whitespace cleanup, case transforms, pattern formatting, and predictable cursor remapping after text changes. | Host apps own field meaning, localized copy, business validation, accessibility announcements, persistence, analytics, and server/domain rules. |
| `./PackagesForReuse/AppIntentSupport` | Yes | `AppIntentSupport` | Reusable App Intents support mechanics: validation/normalization/package marker. | Use when app-owned App Intents need shared helper behavior such as intent-safe text normalization, required-text validation, length enforcement, and package-level composition markers without moving product actions into the package. | Host apps own concrete AppIntent types, shortcut phrases, dialogs, app-group writes, feature execution, privacy, and Siri/Shortcuts UX. |
| `./PackagesForReuse/AppLifecycle` | Yes | `AppLifecycle` | App lifecycle event/state helper mechanisms. | Use when runtime code needs reusable lifecycle state snapshots, foreground/background/activation events, lifecycle observers, or testable lifecycle-driven coordination without hard-coding scene callbacks everywhere. | Host apps own what work is triggered by lifecycle events, background task policies, session refresh, analytics, and user-visible recovery. |
| `./PackagesForReuse/AppLocalization` | Yes | `AppLocalization` | Localization provider and string lookup mechanics. | Use when reusable code needs a product-neutral localization facade, bundle/provider lookup, locale override support, and deterministic missing-key behavior without owning actual app strings. | Host apps own string catalogs/resources, translation quality, fallback locale policy, pluralization/copy review, and user-facing text. |
| `./PackagesForReuse/AppLogging` | No | `AppLogging` | Structured logging primitives with privacy-aware metadata. | Use when code needs product-neutral log events, levels, categories, redaction, no-op/memory/console/OSLog reporters, or package-safe logger contracts. | Host apps own subsystem/category taxonomy, log volume, privacy classification, export to observability/crash tools, and production retention policy. |
| `./PackagesForReuse/AppNavigation` | Yes | `AppNavigation` | Navigation routes/events/snapshots and diagnostic contracts. | Use when apps need reusable route values, tab/navigation snapshots, persistence contracts, event reporting, and diagnostics while keeping the actual product route graph outside the package. | Host apps own concrete routes, deep-link semantics, screen composition, navigation restoration policy, and user-facing routing errors. |
| `./PackagesForReuse/AppNetworking` | Yes | `AppNetworking` | API request/response, retry, auth refresh, upload/download/offline queue mechanics. | Use when a host app needs reusable HTTP transport: request models, URLSession execution, cancellation, retry sleepers, interceptors, coalesced auth refresh, rich HTTP failure context, mock clients, uploads/downloads, and offline queue primitives. | Host apps own endpoints, DTO/domain mapping, token storage policy, backend semantics, user-facing errors, telemetry, and privacy review. |
| `./PackagesForReuse/AppObservability` | No | `AppObservability` | Observability, spans, breadcrumbs and diagnostic reporting primitives. | Use when code needs reusable trace/span IDs, breadcrumbs, duration measurement, cancellation/failure descriptors, privacy-aware attributes, and correlation propagation without choosing a vendor. | Host apps own telemetry names, sampling/export adapters, crash/analytics integration, retention, and domain-specific attribute classification. |
| `./PackagesForReuse/AppOnDeviceAI` | Yes | `AppOnDeviceAI` | On-device AI abstractions/helpers used by app features. | Use when features need product-neutral local AI capability checks, prompt/request/result contracts, translation-like task execution, availability reporting, and safe fallback surfaces. | Host apps own prompt wording, field extraction, target language selection, AI result presentation, persistence, privacy disclosures, and cloud-vs-local policy. |
| `./PackagesForReuse/AppPagination` | No | `AppPagination` | Generic pagination state/load/merge mechanics. | Use when lists need reusable cursor/page/offset state, load direction handling, in-flight state cleanup, merge validation, completed-load counters, and overflow-safe pagination progression. | Host apps own endpoint semantics, DTO/domain mapping, retry/auth, UI loading/error states, telemetry, and exact previous-page behavior where backend anchors are domain-specific. |
| `./PackagesForReuse/AppPermissions` | Yes | `AppPermissions` | Permission status/request helper mechanics for Apple platform capabilities. | Use when features need a reusable permission status model, request boundary, denial/restricted states, and testable permission managers for capabilities such as photos, camera, notifications, or files. | Host apps own Info.plist copy, education screens, fallback UX, analytics, and which capabilities are requested at which moment. |
| `./PackagesForReuse/AppPushNotifications` | Yes | `AppPushNotifications` | APNs token/notification registration and event handling helpers. | Use when push code needs reusable registration state, APNs token handling, payload/event modeling, failure reporting, and testable notification boundaries. | Host apps own notification categories, payload routing, deep links, permission copy, backend registration semantics, and user-visible notification behavior. |
| `./PackagesForReuse/AppRateLimiter` | No | `AppRateLimiter` | Fixed-window/token-bucket rate limiting. | Use when local or client-side actions need reusable rate limit evaluation, costs, reset timing, token buckets, fixed windows, throwing stores, and deterministic clock-based tests. | Host apps own fail-open/fail-closed policy, user messaging, key cardinality, durable storage backend, telemetry, and server-side coordination. |
| `./PackagesForReuse/AppRemoteAssets` | No | `AppRemoteAssets` | Remote asset manifest validation and retrieval mechanics. | Use when a product has remote asset manifests that require HTTPS-by-default validation, checksum/size/status-code checks, sanitized diagnostics, bounded response data, and cancellation-preserving transport. | Host apps own manifest schema meaning, asset rollout, storage/cache policy, UI fallback, authentication, and content moderation/privacy decisions. |
| `./PackagesForReuse/AppSecureStorage` | No | `AppSecureStorage` | Secure storage/keychain-style boundary for tokens and secret-like values. | Use when small sensitive values need async secure storage contracts, Keychain-backed Apple storage, in-memory test/simulator stores, sanitized errors, and key/value validation. | Host apps own token taxonomy, refresh/logout policy, migration from old stores, access group/entitlement decisions, and user-facing auth recovery. |
| `./PackagesForReuse/AppShareExtensionSupport` | Yes | `AppShareExtensionSupport` | App-group JSON storage and share-extension import support. | Use when app extensions need reusable app-group JSON item stores, pending-item transfer, corrupted-item quarantine, and NSItemProvider import support for text/image/video/pdf/audio/file payloads. | Host apps own share composer UI, app-specific draft/card payloads, authentication gating, import-to-runtime policy, and user-visible extension copy. |
| `./PackagesForReuse/AppStateMachine` | No | `AppStateMachine` | Generic async state-machine runtime with persistence hooks. | Use when a domain has explicit states/events, guarded transitions, async actions, revisioned snapshots, store-backed persistence, serialized sends, and failure-visible transition boundaries. | Host apps own state/event taxonomy, side-effect idempotency, compensation strategy, UI presentation, telemetry, and product recovery rules. |
| `./PackagesForReuse/AppSync` | No | `AppSyncCore, AppSyncObservation` | Sync engine mechanics plus optional observable status layer. | Use when data flows need reusable sync orchestration: push/pull phases, mutation queues, cursors, conflict contracts, retry/status reporting, and an optional Observation-backed UI status store. | Host apps own payload mapping, endpoint semantics, local schema application, merge/conflict policy, offline UX, and data-loss guarantees. |
| `./PackagesForReuse/AppTaskQueue` | No | `AppTaskQueue` | Durable task queue, reservation, retry and payload-limit mechanics. | Use when local work needs durable queued tasks with payload validation, media types, reservation/lease semantics, retry policy, completion/failure transitions, and crash-recovery boundaries. | Host apps own concrete task types, durable store backend atomicity, multi-runner policy, payload privacy, telemetry, and user-visible retry/error UI. |
| `./PackagesForReuse/AppUploads` | No | `AppUploads` | Secure generic upload service with URL, size, retry and cancellation policies. | Use when a product needs standalone upload mechanics: HTTPS-by-default URL validation, payload size limits before file reads, multipart/file/data payloads, cancellation preservation, retry sleeper injection, and response limits. | Host apps own upload endpoints, authentication, progress UI, media processing, retry copy, backend response mapping, and privacy policy. |
| `./PackagesForReuse/AppValidationCore` | No | `AppValidationCore` | Lower-level validation engine and safe validation identifiers. | Use when multiple validation surfaces need typed validation values, safe identifiers, reusable rule evaluation, missing-value detection, duplicate-value rejection, and machine-readable issue codes below a form controller layer. | Host apps own business rules, localized messages, field labels, accessibility phrasing, backend validation, and UI state presentation. |
| `./PackagesForReuse/AppWidgetSupport` | Yes | `AppWidgetSupport` | Generic widget snapshot storage/support. | Use when widget extensions need reusable snapshot storage, UserDefaults/app-group handoff, encoded payload persistence, and generic widget data transfer mechanics independent of app-specific widget content. | Host apps own widget timelines, payload schema, display policy, privacy, reload scheduling, and exact widget UI. |
| `./PackagesForReuse/TchopProductLocalizationResources` | Yes | `TchopProductLocalizationResources` | TchopApp-specific localization resources; portable with TchopApp but not generic. | Use with TchopApp-family targets that need the same product strings and resources. It is packaged for portability with the app, not as a neutral infrastructure package for unrelated products. | Host apps outside TchopApp should not adopt this as reusable infrastructure; they should provide their own localization resources or bridge them through AppLocalization. |

## Integration Helpers

Integration helpers intentionally compose two or more root packages while keeping root packages independent. Use them only when both sides are already intentionally adopted.

| Helper | Active In TchopApp | Products | Short Summary | Expanded Description | Host/App Keeps |
| --- | --- | --- | --- | --- | --- |
| `./PackagesForReuse/IntegrationHelpers/AppAnalyticsNavigationIntegration` | Yes | `AppAnalyticsNavigationIntegration` | Connects AppNavigation events to AppAnalytics reporting. | Use when both AppNavigation and AppAnalytics are adopted and navigation diagnostics should become analytics events through a reusable mapping layer. | Host apps own which navigation events are tracked, event names, user/session metadata, and privacy/sampling policy. |
| `./PackagesForReuse/IntegrationHelpers/AppAnalyticsNetworkingIntegration` | Yes | `AppAnalyticsNetworkingIntegration` | Connects AppNetworking events/failures to AppAnalytics reporting. | Use when both AppNetworking and AppAnalytics are adopted and transport-level request/failure events should be reported consistently without coupling networking core to analytics core. | Host apps own endpoint redaction, status/error aggregation, sampling, and whether network failures are user/session correlated. |
| `./PackagesForReuse/IntegrationHelpers/AppAnalyticsPushNotificationsIntegration` | Yes | `AppAnalyticsPushNotificationsIntegration` | Connects AppPushNotifications events to AppAnalytics reporting. | Use when both AppPushNotifications and AppAnalytics are adopted and push registration/payload/interaction events need a reusable analytics bridge. | Host apps own notification event taxonomy, payload redaction, campaign attribution, and user consent/privacy policy. |
| `./PackagesForReuse/IntegrationHelpers/AppErrorsNetworkingIntegration` | No | `AppErrorsNetworkingIntegration` | Maps AppNetworking failures into AppErrors surfaces. | Use when both AppNetworking and AppErrors are adopted and transport/HTTP failures need consistent app-level error mapping while keeping the root packages independent. | Host apps own localized copy, endpoint-specific recovery, support actions, and product severity decisions. |
| `./PackagesForReuse/IntegrationHelpers/TchopProductLocalizationResourcesAppLocalizationIntegration` | No | `TchopProductLocalizationResourcesAppLocalizationIntegration` | Bridges TchopProductLocalizationResources into AppLocalization. | Use only for TchopApp-family targets that intentionally combine the product-specific string bundle with the generic AppLocalization lookup/provider mechanics. | Unrelated apps should provide their own resource package and bridge instead of depending on Tchop-specific resources. |

## Selection Rules

1. Start with the package whose problem statement matches the current feature.
2. Prefer one direct package API over app-local wrappers, facades or duplicate managers.
3. Keep product policy in the app: UI copy, routing, DTO mapping, persistence schema, secrets, privacy and rollout decisions.
4. Use integration helpers only for cross-package mapping; do not move app policy into helper packages.
5. Before adding a package to `./PackagesInUse`, verify the vault package and then run app/project verification after source-only wiring.

## Documentation Rule For Future Packages

Every new reusable package must include:

- a package-level `README.md` with summary, solved problem, capabilities, usage, local SwiftPM setup, remote SwiftPM setup, source-only integration notes and verification;
- `PackageContract.md` defining ownership and boundaries;
- `REUSE.md` for copy/adoption guidance;
- package-local `Scripts/verify_package.sh` where applicable;
- an entry in this `./PackagesForReuse/PACKAGE_CATALOG.md` with a useful expanded description, not a two-word label;
- if active in TchopApp, a matching `./PackagesInUse/<PackageName>/README.md` and `./PackagesInUse/PACKAGE_CATALOG.md` entry with the same description quality.
