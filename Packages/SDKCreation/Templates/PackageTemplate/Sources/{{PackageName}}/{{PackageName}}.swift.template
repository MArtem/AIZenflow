import Foundation

/// Root namespace for `{{PackageName}}` package-level metadata.
public enum {{PackageName}}Info {
    public static let packageName = "{{PackageName}}"
}

/// Replace this sample protocol with the package's real public API.
public protocol {{PackageName}}Managing: Sendable {
    func healthCheck() async -> {{PackageName}}HealthStatus
}

/// A small sample value type used by the template.
public struct {{PackageName}}HealthStatus: Sendable, Equatable {
    public let isAvailable: Bool

    public init(isAvailable: Bool) {
        self.isAvailable = isAvailable
    }
}

/// A no-op implementation useful for tests, previews, and disabled configurations.
public struct Noop{{PackageName}}Manager: {{PackageName}}Managing {
    public init() {}

    public func healthCheck() async -> {{PackageName}}HealthStatus {
        {{PackageName}}HealthStatus(isAvailable: false)
    }
}
