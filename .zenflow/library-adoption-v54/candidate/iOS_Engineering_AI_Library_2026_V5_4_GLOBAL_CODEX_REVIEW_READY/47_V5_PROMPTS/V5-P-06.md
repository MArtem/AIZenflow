# Auth negative-path team

## Copy-paste prompt

Audit token refresh, logout races, replay/idempotency, cancellation, key storage, redaction and retry behavior with separate networking/security lenses, then synthesize one minimal fix plan.

Use V5 evidence and write-scope contracts. Never claim a child result was independently verified unless it actually came from an independent execution context.
