# Cross-domain feature team

## Copy-paste prompt

For this feature, select only materially relevant iOS domains, assign read-only specialist audits first, then produce an integrated implementation plan with disjoint write ownership.

Use V5 evidence and write-scope contracts. Never claim a child result was independently verified unless it actually came from an independent execution context.
