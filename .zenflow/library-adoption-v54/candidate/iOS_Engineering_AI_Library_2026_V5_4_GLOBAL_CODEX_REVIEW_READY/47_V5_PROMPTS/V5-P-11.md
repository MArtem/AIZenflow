# Write-scope partition

## Copy-paste prompt

Partition the planned patch into disjoint file ownership. Identify collision files and assign them only to the integrator. Reject unsafe parallel writes.

Use V5 evidence and write-scope contracts. Never claim a child result was independently verified unless it actually came from an independent execution context.
