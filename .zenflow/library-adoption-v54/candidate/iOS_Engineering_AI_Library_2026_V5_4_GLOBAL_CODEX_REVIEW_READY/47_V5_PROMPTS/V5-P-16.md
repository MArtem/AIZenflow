# Integration review

## Copy-paste prompt

Inspect all child patches as one system. Find API mismatches, duplicate abstractions, target-membership errors, shared-state conflicts and missing cross-slice tests.

Use V5 evidence and write-scope contracts. Never claim a child result was independently verified unless it actually came from an independent execution context.
