# First repository adaptation

## Procedure
Run static intake, generate PROJECT_MODEL, render docs, review unknowns, optionally use read-only Xcode discovery, then promote only confirmed facts to hand-maintained project context.

## Exit criteria
Facts and unknowns are explicit; generated artifacts match the current fingerprint; no product files were modified by adaptation; recommended next engineering skill is named.
