# Review Findings Matrix — V5.4 GLOBAL CODEX REVIEW READY

Status language in this document is intentionally limited to this corrective candidate and its shipped synthetic tests. **CLOSED_IN_CANDIDATE is not independent acceptance.** Independent re-review is still required.

| ID | Severity | Root cause | Patch | Regression test / evidence | Observed result | Status |
|---|---|---|---|---|---|---|
| F01 | P1 | Blacklist guard started from allow/read-only and did not model shell/Git argv safely. | `protection.py`: strict argv allowlist, default `REVIEW_UNSUPPORTED`, shell meta/substitution/interpreters/network/dependency/release excluded, conservative Git-global-option parser, no raw command. | `test_F01_guard_unknown_mutation`, `test_F01_git_global_options`, `test_F01_safe_git` | unsafe/unknown forms not green; safe status/diff green | CLOSED_IN_CANDIDATE |
| F02 | P1 | `exists() && is_symlink()` followed dangling links; write/privacy boundary was TOCTOU-prone. | dir-fd/no-follow component walk, final no-follow check, private temp file, fsync + same-dir atomic publish, permission verification; marker/state writes use same primitives. | dangling/existing/parent/race-like symlink tests; permission-failure and private-mode tests | outside targets preserved; chmod failure rejected; 0700/0600 observed | CLOSED_IN_CANDIDATE |
| F03 | P2 | Single mutable `BASELINE.json` had no immutable session lifecycle/identity. | UUID session files; session schema 3; immutable baseline hash; active/verified/closed lifecycle; all lifecycle transitions serialized under one Git-common-dir lock; persistent writer lease prevents linked-worktree concurrent writers; invalid/foreign state fails closed. | re-begin, concurrent begin, deterministic verify/close and two-verify barriers, real linked-worktree admission, malformed/foreign session tests | closed is terminal under tested interleavings; one writer per Git common-dir; independent repos remain independent | CLOSED_IN_V5_4_CANDIDATE |
| F04 | P1 | Broad `allow-git-metadata` suppressed HEAD/refs/index/config checks and could hide out-of-scope commits. | Removed broad flag; explicit `stage`/`commit` transitions; baseline-tree→current-tree diff; individual refs/config/branch/index/staged/dirty-baseline checks. | commit inside/outside scope, config/ref/branch/staging, dirty-baseline, and pre-existing staged-blob semantic-index tests | only declared in-scope commit transition passes; staged blob changes are not hidden by unchanged staged path names | CLOSED_IN_CANDIDATE |
| F05 | P1 | Nested repo snapshot hashed status text, not semantic dirty/staged content. | Recursive nested boundary captures identity, HEAD, refs, local config, semantic index, changed/staged paths and dirty hashes. | nested dirty→dirty, nested staged-content, recursive second-level, and nested `.git` symlink fail-closed tests | same-status different-content mutation detected; recursive nested boundary observed; unsafe nested `.git` symlink rejected as incomplete | CLOSED_IN_CANDIDATE |
| F06 | P2 | Observation failures were encoded as ordinary fingerprints and could compare equal. | Typed `ObservationError`; bounded subprocess/file observation; malformed/timeout/budget/partial paths return NON_PASS/incomplete; unborn state distinct. | Git observation failure, budget exhaustion, timeout, malformed output tests | incomplete observation never PASS | CLOSED_IN_CANDIDATE |
| F07 | P2 | `total == 0` implied build graph safety. | Scanner returns status/coverage/findings/errors/limitations with `review_required=true`, `safe_to_assume_read_only=false`; inspects PBX shell phases, scheme actions and SwiftPM plugin/macro hints using bounded no-follow reads. | empty, malformed, symlink-input and known executable-surface tests | zero findings never means safe; known surfaces reported; symlink inputs are partial/not followed | CLOSED_IN_CANDIDATE |
| F08 | P1 | Raw guard input and command-derived adapter text could retain secrets. | Guard returns hash/safe executable category/classification/fixed reason code and fixed safe reason text only. Adapter retains bounded metadata/line hash, never source command body; quoted/leading assignments, headers/URL/token-like inputs minimized. | guard secret tests; quoted/leading assignment; header+URL credential tests | synthetic secrets absent from serialized outputs | CLOSED_IN_CANDIDATE |
| F09 | P2 | Context fingerprint omitted producer inputs/options/generator identity and could remain falsely fresh. | Adapter identity includes all inspected producer metadata, README/CONTRIBUTING/scripts/instructions where used, adapter/schema/options/budgets; partial/skipped/too-large/unreadable prevents complete freshness. | README command invalidation, generator version invalidation, budget/symlink/nested-boundary tests | producer/version changes invalidate; repeated stable partial remains non-fresh/non-zero | CLOSED_IN_CANDIDATE |
| F10 | P2 | Installer updated destinations before complete collision/ownership analysis; uninstall/update could destroy user edits. | Full preflight before mutation; reference/full modes; ownership hashes; managed/unmanaged collisions; staging/rollback; update only unchanged managed assets; manifest-based safe uninstall preserving unknown/modified content and exact existing AGENTS bytes. | late collision, state-marker collision, mid-install/update rollback, local edit preservation, unknown file preservation, exact AGENTS preservation, no-follow metadata deletion, full-install validator | no tested partial install/overwrite; user edits preserved or operation blocked | CLOSED_IN_CANDIDATE |
| F11 | P2 | Validation claims were not backed by shipped executable regression tests. | `tests/run_all.py` + `tests/test_review_ready.py`, standard-library synthetic fixtures; root report records only observed suite results. | complete shipped suite | 173/173 PASS, 0 FAIL, 0 SKIP on the V14 corrective working tree | CLOSED_IN_V14_CANDIDATE |
| F12 | P2 | CP-03 promoted weak self to strong self before suspension and did not fully gate stale failures. | Conceptual pattern captures dependency independently, checks cancellation after await, reacquires owner only at UI update, uses generation/replacement gate and cancellation-aware generic failure handling. | `test_F12_task_lifetime_pattern_semantics_documentation` | prohibited early `guard let self` absent; required gates present | CLOSED_IN_CANDIDATE |
| F13 | P2 | English substring heuristic defaulted unknown tasks to R1. | Explicit `--risk`/`--domain`; heuristic uncertainty => `UNKNOWN`; RU destructive markers supported as guardrail; partial repository evidence denies delegation; task text hashed only. | unknown-language, Russian destructive migration, explicit risk, partial-evidence tests | unknown not downgraded; explicit risk respected; partial evidence fails closed | CLOSED_IN_CANDIDATE |
| F14 | P2 | Generic `ios-*` skill names collided with existing project/global skills and authority/precedence was underspecified. | 60 skills namespaced `ioslib-*`; reference default installs no skills; full requires matching dry-run preflight; local/project rules preserved; knowledge not permission authority. | namespace/internal-reference, generic-collision, dry-run/no-mutation, reference-no-skills tests | 60/60 namespaced; generic user skill preserved | CLOSED_IN_CANDIDATE |
| K01 | quality | File count/boilerplate overstated domain depth. | Shared generic workflow moved to `COMMON_EXECUTION_CONTRACT.md`; repeated placeholder 71→0; priority task-lifetime/cancellation/retry/auth/SwiftUI state+identity/migration/memory/security/accessibility/performance docs include scenario, wrong/preferred approach, traps/verification and primary sources. | K01 placeholder/depth tests | old repeated phrase count 0; 12 priority documents satisfy depth/source checks | ADDRESSED_IN_CANDIDATE |
| K02 | quality/safety | Unbounded traversal/read/output could hang or produce false complete state. | Protection/adapter/scanner budgets for visits, inspected files, bytes, per-file bytes, subprocess output/time, deadline and adapter depth; exhaustion => incomplete. | adapter budget, protection budget/timeout, symlink/nested boundary tests | budget exhaustion/partial scan never complete/PASS | ADDRESSED_IN_CANDIDATE |
| K03 | security claims | Snapshot detection was conflated with prevention/backup/ignored/transient coverage. | README/architecture/global block/runtime result language separates prevention/detection/advisory and explicitly excludes backup, kernel sandbox, transient-write and complete ignored-file guarantees. | `test_K03_guarantee_terminology_present` | limitation terminology present and runtime status uses detection wording | ADDRESSED_IN_CANDIDATE |
| K04 | quality/privacy | Legacy Swift scanner had broken `try!` regex, unsafe reads and emitted source lines. | Bounded heuristic scanner, no-follow/symlink skip, corrected patterns, metadata leads only, no source body, incomplete on observation errors. | `test_K04_swift_risk_scan_no_source_body` | `try!` lead found without source body emission | ADDRESSED_IN_CANDIDATE |
| K05 | provenance/trust | Artifact hash was conflated with correctness and repository-derived instructions could be treated as authority. | `GLOBAL_MANIFEST.json` pins the V5.2 reviewed input SHA, prior V5.1/original V5 identities, release/test identity and archive-hash caveat; adapter marks commands `data_only`; global docs separate knowledge/runtime/instruction authority; primary-source mapping retained. | `test_K05_provenance_and_authority_manifest`; package validator | provenance/authority fields verified; no correctness claim from SHA | ADDRESSED_IN_V5_3_CANDIDATE |

## New defect found during this iteration

Two integration defects were caught while strengthening release validation rather than being hidden by documentation. First, `validate_global_install.py` still reflected the obsolete HARDENED registry model and was rewritten around the current ownership model, no-follow path validation, private state, AGENTS integrity, reference/full modes and all 60 `ioslib-*` skills. Second, the installed registry did not explicitly persist the authority booleans `knowledge_is_permission_authority=false` and `client_repo_install_required=false`; install and sync now persist them and the validator enforces them. Synthetic full-install validation and rollback tests pass.

## A54 corrective findings

| ID | Severity | Root cause | Corrective patch | Regression evidence | Current status |
|---|---|---|---|---|---|
| A54-R1 | P1 | Cleanup failure after the first backup deletion entered rollback with an already-missing backup and could remove part of the published installation. | `sync_global.py` defines the publication commit point; post-publication backup cleanup is best effort and returns `SyncCleanupIncomplete` without rollback. | `test_F10_late_backup_cleanup_failure_preserves_published_state`; direct CLI exception/status check | CLOSED_IN_CORRECTIVE_CANDIDATE; independent review pending |
| A54-R6 | P1 | Uninstall journal entries and the cleanup commit point were recorded too late, so post-rename/fsync or partial backup cleanup could be reported as a complete rollback. | `uninstall_global.py` pre-journals tree/metadata publication, restores only exact owned bytes before commit, and reports `UninstallCleanupIncomplete` after cleanup starts. | post-rename, post-unlink and late-cleanup uninstall fixtures | CLOSED_IN_CORRECTIVE_CANDIDATE; independent review pending |
| A54-R2 | P2 | Final legacy-record validation occurred after the last deadline checkpoint and could return success beyond the cooperative envelope. | `ios_ai.py` checks budget after validation and before successful return; per-file limit is explicit and does not get widened by aggregate remaining bytes. | slow-read, slow-validation, aggregate-boundary and per-file-boundary tests | CLOSED_IN_CORRECTIVE_CANDIDATE; cooperative deadline only |
| A54-R3 | P2 verification | File-replacement fixture failed on non-private mode before exercising identity, so bypassing identity still passed. | Replacement fixture sets private mode and asserts the no-follow loader's identity error; bounded in-memory bypass now fails the test. | race test plus mutation-bypass negative control | CLOSED_IN_CORRECTIVE_CANDIDATE |
| A54-R4 | acceptance gap | Prior L2 packet combined inputs/key, did not freeze hashes and omitted the holdout key/criterion. | Corrective plan requires separated reviewer/evaluator packets and predeclared score gate; preparation remains task-local until independent execution. | `evidence/12-l2-code-review-pilot-packets.md` supersession and corrective packet work | OPEN_PENDING_INDEPENDENT_RUN |
| A54-R5 | integration gap | Prior L5 material was prose/triage and installed reference semantics were not a passive knowledge route. | Task-local `proposed-integration/` contains exact target paths/hashes, apply-checkable canonical route patch, disable semantics and separate runtime packet. | `proposed-integration/canonical-route.patch` applies with `git apply --check` to canonical base | OPEN_PENDING_CANONICAL_PROMOTION_AND_REHEARSAL |


## V5.1 independent re-review residual findings

The September 11 V5.1 independent review identified V51-01…V51-08. Status below means patched and exercised by the shipped synthetic V5.2 suite, **not independently accepted**.

| ID | Severity | Invariant / root cause | V5.2 patch | Regression fixture | Observed result | Status |
|---|---|---|---|---|---|---|
| V51-01 | P1 | `stage` may authorize the operation class but must never waive write/dirty/protected scope for any semantic index delta. | Index delta is always computed by path+stage+mode+OID; additions/removals/mode/blob changes under `stage` require `allow` or `allow_dirty` plus protected scope where applicable. | existing `MM` blob replacement outside/inside dirty scope; staged removal; staged mode change; stage+commit | out-of-scope/pre-existing index mutation detected; explicitly scoped variants pass | CLOSED_IN_V5_2_CANDIDATE |
| V51-02 | P1 | A partial observation can never become fresh merely because the same partial fingerprint repeats. | `fresh = same && !old_partial && !current_partial`; stable partial returns non-zero and is not redundantly regenerated until identity/options change. | two consecutive partial `context --ensure`, then limit removal | both partial ensures non-zero/non-fresh; complete observation restores fresh | CLOSED_IN_V5_2_CANDIDATE |
| V51-03 | P1 | Basename protection rules apply at every repository depth, while slash-bearing rules remain path-scoped. | `protected()` separates basename patterns from path patterns with normalized separators. | nested Info.plist/Package.swift/PrivacyInfo/xcconfig; mixed separators; false-positive controls; end-to-end nested protected mutation | nested protected files require `allow_protected`; unrelated names do not match | CLOSED_IN_V5_2_CANDIDATE |
| V51-04 | P2 | Positive classification requires executable identity, not a spoofable basename. | `git`/`pwd` green path resolves executable identity conservatively; arbitrary relative paths and PATH-spoofed executables are review-only. | `./untrusted/git`, fake PATH git, relative pwd, trusted `/usr/bin/git` when available | spoofed executables never green; trusted system Git remains green | CLOSED_IN_V5_2_CANDIDATE |
| V51-05 | P2 | Diagnostic output must not echo unknown executable/option/subcommand tokens. | Fixed `reason_code` + allowlisted reason text; unknown executable category is `unknown`; serialized output retains command hash only. | synthetic secrets in global option, subcommand, executable and parse error | synthetic token absent from entire serialized guard result | CLOSED_IN_V5_2_CANDIDATE |
| V51-06 | P2 | Same-worktree concurrent writers cannot be independently verified from whole-repo baselines without coordination. | V5.2 narrowed the contract to one writer per worktree, but repeat review found lifecycle and linked-worktree residuals A52-02/A52-03. V5.3 serializes lifecycle plus writer admission by Git common-dir. | original V51 fixtures plus A52-02/A52-03 deterministic barriers and real `git worktree add` fixtures | same/common-dir writers serialize; independent Git common-dirs remain independent; closed session is not resurrected | FOLLOWUP_CLOSED_IN_V5_3_CANDIDATE |
| V51-07 | P2 | Every path after `Popen` must deterministically terminate/reap owned subprocess resources on deadline/error. | V5.2 added outer cleanup, but repeat review found A52-01: leader exit could trigger a blocking synthetic read and suppress process-group cleanup. V5.3 uses nonblocking pipes, real readiness events only, bounded waits, and process-group cleanup independent of leader liveness. | original deadline test plus silent grandchild/inherited-pipe, closed-pipe live-grandchild and read-error cleanup fixtures | observation stays bounded and owned in-group descendants are removed in the tested POSIX scenarios | FOLLOWUP_CLOSED_IN_V5_3_CANDIDATE |
| V51-08 | P1 | Unexpected directory traversal errors make observation incomplete/partial. | `os.walk(onerror=...)` added to nested discovery/build scanner; adapter records walk errors as limitations/partial; package/installer/installed-tree traversals reject unexpected walk errors. | injected `scandir` denial in protection, build scanner, adapter, package scan, managed-tree hashing and installed-tree validator | capture/package/managed-tree paths fail closed; build scan incomplete; adapter partial/non-fresh; installed-tree validation reports the walk error | CLOSED_IN_V5_2_CANDIDATE |


## Additional V5.2 hardening finding

During implementation of V51-08, the same silent traversal-error class was found in installation/package ownership observation. This was not hidden under V51-08; it is tracked separately here.

| ID | Severity | Invariant / root cause | V5.2 patch | Regression fixture | Observed result | Status |
|---|---|---|---|---|---|---|
| V52-01 | P2 | Installer package enumeration, managed-tree hashing and installed-tree validation also used directory walks that could omit an unreadable subtree if traversal failed unexpectedly. | Safety-critical installer/validator walks now provide `onerror`; package and managed-tree observation raise `InstallError`, while installed-tree validation records an explicit error. | injected `scandir` failure in `_package_rows`, `managed_hashes`, and `validate_global_install.check_tree` | installer observations reject the incomplete tree; installed-tree validator returns an error instead of treating the tree as complete | CLOSED_IN_V5_2_CANDIDATE |


## V5.2 independent repeat-review residual findings — A52

The September 11 V5.2 repeat review identified three remaining narrow defects. V5.3 intentionally changes only these runtime/session invariants and release evidence; it does not expand skills, playbooks, the policy engine, or orchestration surface. Status below means patched and exercised by the shipped V5.3 synthetic suite, **not independently accepted**.

| ID | Severity | Root cause / invariant | Minimal V5.3 patch | Deterministic regression fixture | Observed result | Status |
|---|---|---|---|---|---|---|
| A52-01 | P1 | Direct-child exit is not equivalent to pipe EOF or process-group death. Synthetic read events could call blocking `os.read`, and cleanup returned early when the leader had exited. | Subprocess pipes are nonblocking; reads occur only for selector-reported readiness; no synthetic read event is created from `p.poll()`; pipe/process waits remain budgeted; POSIX cleanup targets the owned process group even after leader exit. | silent grandchild holding inherited pipes while leader exits; grandchild with closed pipes; injected pipe-read failure; prior total-deadline child test | inherited-pipe case terminates within budget with NON-PASS; closed-pipe command completes while background descendant is still cleaned; read failure cleans the owned child/group | CLOSED_IN_V5_3_CANDIDATE |
| A52-02 | P2 | `verify` loaded and saved lifecycle state outside the same registry serialization used by `close`, so stale verify data could resurrect a closed session or lose audit records. | `verify`, `close`, and `begin` share one common-dir lifecycle lock; `_verify_session_locked` avoids nested locking; session schema 3 plus writer lease ties lifecycle transitions to one writer admission record. | barrier-controlled verify/close interleaving; two concurrent verifies; close then replacement begin | close waits for in-flight verify; final old session remains closed; both verify audit records survive; replacement writer starts only after lease release | CLOSED_IN_V5_3_CANDIDATE |
| A52-03 | P2 | Linked worktrees have distinct worktree identities but share ordinary `refs/*` and common config, so treating them as independent writer slots causes false violations after legal commits. | Writer lock/lease is keyed by resolved Git common-dir, not worktree path. Linked worktrees are serialized before writes; concurrent writers are supported only across independent repos/clones with different common dirs. Ref comparison remains strict. | real `git worktree add` common-dir fixture; linked begin rejection/release; independent-repository fixture; third-party shared-ref mutation fixture | second linked writer is rejected before its session begins; after release the linked worktree can acquire the slot; independent repos still run concurrently; unrelated shared-ref mutation is detected | CLOSED_IN_V5_3_CANDIDATE |


## V5.3 independent repeat-review residual finding — A53

The September 11 V5.3 repeat review independently accepted the A52 code changes at the inspected level and identified one upgrade-state compatibility defect. V5.4 changes only this compatibility path and release evidence; it does not expand skills, playbooks, policy engine, or orchestration.

| ID | Severity | Root cause / invariant | Minimal V5.4 patch | Deterministic regression fixture | Observed result | Status |
|---|---|---|---|---|---|---|
| A53-01 | P2 | V5.3 validated every session JSON against schema 3 before considering lifecycle, so a valid V5.2 schema-2 `closed` record became `invalid` and blocked a new writer after upgrade. Historical closed state must remain trustworthy enough for archive/read admission without becoming current evidence; unresolved old writers must remain fail-closed. | Added a strict read-only schema-2 compatibility reader for `closed` sessions only. It validates canonical UUID, baseline hash, payload shape and exact repository identity; marks the record `legacy-v5.2-closed-archival`/historical-only; never rewrites it or grants a writer lease. Explicit status returns `ARCHIVAL_CLOSED` with `verification: null`. Writer admission scans V5.2 records across state entries sharing the current Git common-dir, so old `active`/`verified` linked-worktree writers block. Malformed/foreign/unknown relevant records remain fail-closed. | same-state V5.2 closed → V5.4 begin; archival audit/no evidence promotion; explicit archival status; V5.2 active+verified rejection; corrupted/foreign/unknown rejection; idempotent repeated upgrade; real linked-worktree closed-history plus shared active legacy writer; bounded deadline/bytes/race/symlink/iteration fixtures | A53 upgrade and bounded-observation fixtures PASS in the shipped suite; old closed JSON remains byte-for-byte unchanged; active/verified old writers are not silently accepted | CLOSED_IN_V5_4_CANDIDATE |

## Local candidate hardening follow-up

| ID | Severity | Root cause / invariant | Candidate patch | Evidence | Status |
|---|---|---|---|---|---|
| REG-54-01 | P3 | Legacy writer admission materialized unbounded registry listings, and its structured active/verified blocker path was unreachable because strict schema validation rejected those records first. Registry observation must be bounded, streaming, and able to distinguish a valid legacy writer blocker from archival history without admitting it. | Added explicit legacy-active observation mode for the scanner, streaming directory iteration, aggregate repository/record/byte/deadline envelopes, fail-closed conversion of scan errors, intermediate directory checks and a bounded no-follow aggregate loader. Current `validate_session` semantics remain strict; only the read-only admission scanner uses the compatibility mode. | macOS measurements for 10/1,000/10,000 records; dedicated repository/record/byte bound fixture; shipped malformed JSON, exact-boundary/overflow, replacement-race, iteration-failure, symlink and final-deadline fixtures; full 143-test suite | ADDRESSED_IN_LOCAL_CANDIDATE; INDEPENDENT_REVIEW_REQUIRED |

## V14 independent-review follow-up findings

The bounded Astra review of V13 found two unsafe manual lifecycle assumptions, one missing
protection-upgrade admission contract, one external-layout gap, one knowledge-route wiring gap,
and evidence that overreached the artifact under review. The V14 candidate addresses the local
candidate corrections below; this table is not an independent acceptance.

| ID | Severity | Root cause / invariant | V14 corrective patch | Regression evidence | Status |
|---|---|---|---|---|---|
| AV13-01 | P1 | Manual `ditto` could replace an occupied target without proving ownership. | Shipped read-only `manual_preflight.py` rejects unknown/modified shim, unmarked non-empty state, symlink components and skill collisions before the runbook's ordinary operations. | occupied shim and unmarked state synthetic fixtures; runbook hard stop | CLOSED_IN_V14_CANDIDATE; independent review pending |
| AV13-02 | P2 | Manual shim selected a hard-coded active payload and did not switch to a new external release. | Shim consumes validated `INSTALLATION.json`; A/B fixture changes only the selector and observes release A then B through the same shim. | manual selector A/B and missing-selector tests | CLOSED_IN_V14_CANDIDATE; independent review pending |
| AV13-03 | P2 | Deployment update had no prerequisite check for active sessions from an incompatible protection runtime. | Installer sync and manual preflight share bounded active/verified session admission; mismatch blocks before mutation, current compatibility `.5` is retained while release identity advances to `.6`. | synthetic old active session blocks both paths; existing V5.2 lifecycle suite remains | CLOSED_IN_V14_CANDIDATE; independent review pending |
| AV13-04 | P2 | Manual external payload/state layout was described but not selected by the actual runtime. | Descriptor carries absolute external knowledge/runtime/state roots; runtime honors `IOS_ENGINEERING_STATE_ROOT` and no CODEX_HOME switch is required for payload selection. | relocated manual runtime and external state doctor tests | CLOSED_IN_V14_CANDIDATE; independent review pending |
| AV13-05 | P2 | Router/profile was prose-only, so duplicate exclusion and ordinary task routing were not executable contracts. | Shipped `KNOWLEDGE_ROUTER.md`, profile schema, bounded exact-hash profile CLI and capability matrix; source drift clears exclusions and partial overlap disables nothing. | profile active/exact/changed-source tests and router/matrix assertions | ADDRESSED_IN_V14_CANDIDATE; independent usefulness/pilot evidence pending |
| AV13-06 | P2 | Current report mixed historical V13/Astra facts with current candidate claims and carried stale counts/timing. | V14 report/manifest use artifact-scoped status, current `173/173` exit evidence and no unmeasured wall-clock claim; historical V13 review is labeled separately. | validator checks report/manifest count consistency; final extracted artifact check pending | ADDRESSED_IN_V14_CANDIDATE; independent review pending |

## Astra V14 review follow-ups

The following statuses describe this candidate's bounded local implementation and observed
synthetic/manual-fixture evidence. They do not grant independent acceptance or actual Codex host
adoption.

| ID | Severity | Corrective result | Evidence | Status |
|---|---|---|---|---|
| AV14-01 | P1 | Prepared the global first-entry block and retained the exact host-entry preview; repository-root bootstrap remains the portable fallback. | `evidence/22-host-entrypoint-preview.md`; candidate `GLOBAL_CODEX/AGENTS.global.block.md`; actual fresh Desktop/session matrix not run | PREPARED_HOST_GATE_PENDING |
| AV14-02 | P1 | Installer and manual preflight reject client-repository destinations, source overlap and independent target overlap before mutation. | `test_F14_installer_rejects_client_repository_destinations`, `test_F14_manual_preflight_rejects_independent_target_overlap`; manual/installer smoke | CLOSED_IN_CANDIDATE |
| AV14-03 | P2 | Manual lifecycle now checks every emitter/publication, resolves active AGENTS override consistently, documents explicit update/disable/rollback and preserves old payload on failure. | `MANUAL_DEPLOYMENT.md`; override/preflight tests; current manual reference smoke | CLOSED_IN_CANDIDATE; host operator update remains separate |
| AV14-04 | P2 | Profile revalidates candidate and source identities and exact duplicate evidence; explicit relative layout mappings are supported; ambiguous mappings fail. | profile tests and `knowledge_profile.py`; schema/router contract | CLOSED_IN_CANDIDATE |
| AV14-05 | P2 | Knowledge/read-only routes no longer create protection writer sessions; protection remains explicit for authorized writes. | `AGENTS.global.block.md`; runtime/acceptance policy tests | CLOSED_IN_CANDIDATE |
| AV14-06 | P2 | Legacy admission/profile observation uses streaming bounded scans, actual-byte accounting, deadlines, no-follow identity checks and fail-closed unknown/error handling. | legacy scanner and profile boundary/race tests; full suite | CLOSED_IN_CANDIDATE |
| AV14-07 | P2 | A→B→A selector now verifies distinguishable payload markers; current candidate received one final suite/validator pass and one copied-runtime/manual smoke. | selector test; `REVIEW_READY_VALIDATION_REPORT.md`; final artifact receipt | ADDRESSED_IN_CANDIDATE; independent review and real pilot pending |
