# Budget & Termination Gate

Pass only when: fan-out/depth/context stay bounded and spawning stops when marginal value disappears.

A failed gate blocks additional fan-out or final integration until resolved or explicitly accepted as residual risk.
