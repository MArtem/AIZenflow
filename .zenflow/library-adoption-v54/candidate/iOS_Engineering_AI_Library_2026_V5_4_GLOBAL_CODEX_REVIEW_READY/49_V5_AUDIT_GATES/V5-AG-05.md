# Integration Gate

Pass only when: integrated diff preserves cross-domain invariants and shared target/build configuration.

A failed gate blocks additional fan-out or final integration until resolved or explicitly accepted as residual risk.
