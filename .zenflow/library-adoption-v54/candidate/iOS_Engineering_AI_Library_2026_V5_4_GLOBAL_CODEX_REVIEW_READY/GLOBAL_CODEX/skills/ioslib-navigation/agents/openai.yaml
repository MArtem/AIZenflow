interface:
  display_name: "iOS Navigation"
  short_description: "Treat routes as stable state with validation, ownership and r"
  default_prompt: "Use $ioslib-navigation to treat routes as stable state with validation, ownership and restoration semantics."
