interface:
  display_name: "Networking & Auth"
  short_description: "Protect API semantics, session ownership and concurrency-safe"
  default_prompt: "Use $ioslib-networking-auth to protect API semantics, session ownership and concurrency-safe recovery paths."
