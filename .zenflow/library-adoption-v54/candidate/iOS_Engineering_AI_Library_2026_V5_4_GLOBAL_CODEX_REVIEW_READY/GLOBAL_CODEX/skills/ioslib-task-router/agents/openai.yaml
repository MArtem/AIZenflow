interface:
  display_name: "iOS Task Router"
  short_description: "Route the task by observable outcome and risk boundary; avoid"
  default_prompt: "Use $ioslib-task-router to route the task by observable outcome and risk boundary; avoid loading the full library."
