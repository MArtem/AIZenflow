interface:
  display_name: "Memory & Lifetime"
  short_description: "Trace the actual retaining path and make lifetime ownership e"
  default_prompt: "Use $ioslib-memory-leaks to trace the actual retaining path and make lifetime ownership explicit."
