interface:
  display_name: "Background Execution"
  short_description: "Design background work around system budgets, idempotency, ca"
  default_prompt: "Use $ioslib-background-execution to design background work around system budgets, idempotency, cancellation, expiration and observable recovery."
