interface:
  display_name: "Parallel Investigation"
  short_description: "Parallel Investigation for iOS repositories"
  default_prompt: "Use $ioslib-parallel-investigation to handle this task with bounded evidence-driven orchestration."
