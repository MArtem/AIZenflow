interface:
  display_name: "App Intents"
  short_description: "Make intent side effects, authentication, availability and en"
  default_prompt: "Use $ioslib-app-intents to make intent side effects, authentication, availability and entity identity deterministic."
