interface:
  display_name: "iOS Performance"
  short_description: "Measure the user-relevant bottleneck and require before/after"
  default_prompt: "Use $ioslib-performance to measure the user-relevant bottleneck and require before/after evidence."
