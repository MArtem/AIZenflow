interface:
  display_name: "Swift Concurrency"
  short_description: "Make isolation and task lifetime correct by design instead of"
  default_prompt: "Use $ioslib-swift-concurrency to make isolation and task lifetime correct by design instead of suppressing compiler diagnostics."
