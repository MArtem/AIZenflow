interface:
  display_name: "iOS Design System"
  short_description: "Treat component semantics and accessibility as stable API, no"
  default_prompt: "Use $ioslib-design-system to treat component semantics and accessibility as stable API, not screenshot-only styling."
