interface:
  display_name: "iOS Crash Debugging"
  short_description: "Localize crash mechanism and violated invariant using symboli"
  default_prompt: "Use $ioslib-crash-debugging to localize crash mechanism and violated invariant using symbolicated evidence and surrounding ownership/lifecycle."
