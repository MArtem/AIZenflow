interface:
  display_name: "Conflict Resolution"
  short_description: "Conflict Resolution for iOS repositories"
  default_prompt: "Use $ioslib-conflict-resolution to handle this task with bounded evidence-driven orchestration."
