interface:
  display_name: "Modularity & SPM"
  short_description: "Create boundaries that reflect ownership and change patterns"
  default_prompt: "Use $ioslib-modularity-spm to create boundaries that reflect ownership and change patterns, not arbitrary folder splitting."
