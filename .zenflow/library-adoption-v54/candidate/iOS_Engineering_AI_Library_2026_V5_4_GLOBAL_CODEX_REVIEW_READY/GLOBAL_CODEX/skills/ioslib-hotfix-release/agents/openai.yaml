interface:
  display_name: "iOS Hotfix"
  short_description: "Patch the proven root cause with containment, telemetry and r"
  default_prompt: "Use $ioslib-hotfix-release to patch the proven root cause with containment, telemetry and reversible rollout."
