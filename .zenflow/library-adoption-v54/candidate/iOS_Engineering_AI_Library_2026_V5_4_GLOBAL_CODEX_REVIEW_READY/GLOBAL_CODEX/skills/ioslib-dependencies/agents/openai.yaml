interface:
  display_name: "iOS Dependency Governance"
  short_description: "Admit dependencies only with a justified boundary, compatibil"
  default_prompt: "Use $ioslib-dependencies to admit dependencies only with a justified boundary, compatibility and exit plan."
