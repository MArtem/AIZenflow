interface:
  display_name: "On-device AI / ML"
  short_description: "Treat model capability as a versioned dependency with privacy"
  default_prompt: "Use $ioslib-ai-ml to treat model capability as a versioned dependency with privacy, evals and deterministic safety boundaries."
