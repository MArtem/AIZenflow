# Current Task Handoff

## Orientation

- Worktree/task: `/Users/Artem/.zenflow/worktrees/new-task-be0b`, `new-task-be0b`
- Active scope: documentation-context economy; no product-code work.
- Canonical repository: `/Users/Artem/.zenflow/worktrees/documentation-vault`
- Mode/model: `эконом`; `GPT-5.6 tera`, low for bounded documentation edits.

**перечитать весь актуальный набор документации и правил для этого worktree и task-контекста**

The current set means Level 0, current task state, selected routes, the current app/project overlay,
and changed or explicitly required documents—not the full library. The canonical definition is in
`docs/CONTEXT_TRANSFER_AND_NEW_CHAT_STANDARD.md`.

## Current State

- Figma normal route was corrected and published as `b257b52`: compact router plus design standards;
  the deep Figma reference loads only on its explicit triggers.
- Context-transfer scope was defined and published as `0a5f9c7`.
- Prior task detail remains recoverable in Git history and the existing task archive.

## Restrictions And Evidence

- Keep all work inside `/Users/Artem/.zenflow`; do not access the secret deny boundary.
- No app build, tests, Simulator, signing, or source/test changes are authorized for this task.
- Canonical documentation commits and pushes are standing-authorized after relevant checks.
- Use targeted documentation checks, manifest/vault validation, and `git diff --check` only.

## Next Safe Step

Decide whether to extend the existing context-cost report to include bootstrap, `AGENTS.md`, Level 0,
task state, and selected route as one measured instruction envelope. Do not create a new engine,
change active project rules, or mass-delete archives.
