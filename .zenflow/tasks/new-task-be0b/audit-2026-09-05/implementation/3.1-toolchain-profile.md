# Implementation receipt — 3.1 toolchain/isolation/availability contract

Date: 2026-09-08  
Model: GPT-5.6 Luna, reasoning xhigh  
Mode: эконом  
Owner: Documentation Vault (reusable contract); consuming projects own profile values

## Scope and authority

This block implemented the roadmap's toolchain/isolation/availability contract as reusable
documentation. It did not change an app's build settings, deployment target, engine, package
source, tests, runtime behavior, CI, or release configuration. The canonical authority is
Documentation Vault commit `aabeeb64a87801d12a31b896624f43c8769d7909` on `origin/main`.

## Contract delivered

- Added `reusable/baseline/docs/IOS_TOOLCHAIN_PROFILE_STANDARD.md` with a project-owned profile for
  Xcode/compiler, Swift language mode, SDK, deployment target, target membership, strict
  concurrency, default actor isolation, upcoming feature stability, Observation, UIKit/SwiftUI
  bridging, availability, iPhone/iPad/window scope, and permitted verification route.
- Kept compiler, language mode, SDK and deployment as separate dimensions; unknown values remain
  unknown instead of receiving a guessed default.
- Scoped Swift 6.2 default MainActor isolation and `@concurrent`, Swift 6.3 Swift Testing
  changes, Observation, UIKit bridging and iPad/window adaptation to the consuming profile.
- Preserved the distinction between asynchronous waiting and CPU-bound work; `async` alone is not
  evidence of background execution. `@MainActor` UI state is not a finding by itself.
- Added availability/fallback/entitlement/hardware/permission/migration/evidence fields and
  explicitly prohibited automatic deployment-target raises or beta APIs in the stable baseline.
- Routed the new standard through the active router and task routes, added `QC.TOOLCHAIN.PROFILE`
  to the catalog, and extended the compatibility matrix with toolchain/isolation/source-evidence
  dimensions.

## Verification

- Canonical manifest regenerated by `python3 -B scripts/generate_manifest.py --write`.
- Documentation vault structural check: PASS (`3736 files; 4 app boundaries`).
- Task router, consistency, boundary and manifest checks were run against the task mirror in the
  prior static pass; no new contradiction was introduced by this block.
- `git diff --check`: PASS before commit.
- Remote receipt: local `HEAD` and `origin/main` both equal
  `aabeeb64a87801d12a31b896624f43c8769d7909`.
- No build, test, Simulator, device, Instruments, CI, signing, or runtime evidence was collected;
  those remain permission-bound later blocks.

## Acceptance and rollback

Roadmap acceptance is met: primary links and review date are present; async wait/CPU-bound work,
valid `@MainActor`, contextual sync worker review and profile-selected optional settings are
distinguished. Rollback is a documentation revision/pin rollback and does not require changing
consumer build settings.

## Residual limits

The reusable standard defines fields and review interpretation. It does not prove any project's
actual toolchain, availability matrix, build, runtime, performance, accessibility or release
readiness until an app-owned profile and permitted evidence exist.

**перечитать весь актуальный набор документации и правил для этого worktree и task-контекста**
