# Implementation block 0.4 — evaluator-only answer key

Date: 2026-09-08
Model: GPT-5.6 Luna, reasoning xhigh
Access: evaluator only; do not route to generator or blind detector

This key is deliberately separate from `0.4-neutral-scenarios.md`. It records one acceptable
behavioral shape and one deliberate defect per scenario. It is not a score, test result, or proof
that a model detected the defect. S04 and S05 remain holdout candidates until block 7.2.

| ID | Acceptable variant | Deliberate defect for detection | Detection boundary |
| --- | --- | --- | --- |
| S01 | State accepts a result only when it still belongs to the current request identity; obsolete A cannot replace B. | Completion of A writes state unconditionally after B already became current. | A finding must identify ordering/identity failure, not prescribe an unapproved architecture. |
| S02 | Cancellation propagates, prevents stale mutation, and is classified separately from user-visible failure. | A broad error catch converts normal cancellation into a failure alert and leaves a stale mutation path. | A finding must distinguish cancellation from recoverable failure. |
| S03 | Existing valid content remains, freshness is honest, and retry is explicit after transient failure. | Refresh failure clears valid content and reports an empty state with no retry. | A finding must cover data preservation and recovery affordance. |
| S04 | Save uses a failure-safe replacement boundary; old durable value remains readable when new save fails. | Old value is deleted or overwritten before the new value is durably confirmed. | Holdout; no tuning or normal detector PASS before fresh context and block 7.2. |
| S05 | Required-field/unknown-enum validation rejects the payload before domain success. | Missing field receives a fabricated default or unknown enum is silently mapped to a valid value. | Holdout; no tuning or normal detector PASS before fresh context and block 7.2. |

## Key handling

The evaluator records generator quality separately from detector quality. Findings are captured
before this key is disclosed to the detector. If the key was visible, mark the result `unblinded`
and do not call it independent holdout evidence. No runtime, tests, scoring service, or benchmark
runner is created by this document.
