# Implementation block 8.2 — Tchop multi-target consumer pilot (partial)

Date: 2026-09-08  
Model: GPT-5.6 Luna, reasoning xhigh  
Mode: эконом  
Consumer: `/Users/Artem/.zenflow/worktrees/new-task-be0b`  
Consumer HEAD: `b77e8dc2bbe37bec0d652c9a5bbd8c7e024011ca`  
QualityControl engine under review: `b197bd5e8983b5c7cfd1d277dd2540d7bb352a15`

## Contract and scope

This block evaluates the existing multi-target profile and the static half of the pilot. The
profile covers `TchopApp`, share/widget extension source, `PackagesInUse`, and two app schemes.
The worktree was clean before the probe. No Swift source, test source, project membership,
entitlements, lifecycle behavior, or runtime was changed.

The full phase-8 matrix remains mandatory: identity/static, runtime, positive and deliberate
negative controls, dry-run/apply/repeat/rollback, local/GitHub parity, and pre-PR review. Missing
or unavailable rows keep this pilot partial.

## Static evidence

- The local lightweight gate was invoked with a task-sandbox `TMPDIR`; it produced a `BLOCKED`
  receipt because the temporary output directory made the worktree dirty. This is the intended
  clean-checkout boundary, not a PASS claim.
- On clean `HEAD`, the seven QC adapters passed except `QC.LOCALIZATION.CATALOG`:
  `QC.SECRETS.TRACKED`, `QC.TODO.OWNER`, `QC.GENERATED.OWNERSHIP`,
  `QC.DEPENDENCY.LOCK_DRIFT`, `QC.RESOURCES.ASSETS`, and `QC.PRIVACY.MANIFEST` were PASS.
- `QC.LOCALIZATION.CATALOG` failed in both the active and vault copies of
  `TchopProductLocalizationResources`. Relative to the English fallback, the German locale is
  missing 267 keys in `PackagesInUse` and 261 keys in `PackagesForReuse`; 35 are in the
  accessibility namespace. The first reported keys include channel/FAB/feature/login/news
  accessibility labels and hints.
- A bounded attempt to add the initially reported parity keys exposed the next deterministic
  tranche of missing keys. The attempt was reverted before commit, so no partial localization
  was published and the consumer worktree is clean.

## App-local remediation continuation — 2026-09-08

The app owner selected Option A from `8.2-german-locale-contract.md`: German remains unsupported
until a complete reviewed catalog exists. The incomplete `de.lproj` was removed from both
`PackagesInUse/TchopProductLocalizationResources` and
`PackagesForReuse/TchopProductLocalizationResources` in consumer commit
`c0e8ec593c6411eb47e3f5075cf90c23200a5d4a`. No English-to-German copying, machine translation,
QC weakening, or reusable `AppLocalization` change was made.

On a clean snapshot of that exact SHA, the seven bounded deterministic adapters all returned
`PASS`: `QC.SECRETS.TRACKED`, `QC.TODO.OWNER`, `QC.GENERATED.OWNERSHIP`,
`QC.DEPENDENCY.LOCK_DRIFT`, `QC.RESOURCES.ASSETS`, `QC.PRIVACY.MANIFEST`, and
`QC.LOCALIZATION.CATALOG`. This is static evidence for the clean source snapshot only.

The Tchop `.quality-control/profile.json` and `.github/workflows/manual-quality.yml` were then
aligned to the same immutable QC revision `b197bd5e8983b5c7cfd1d277dd2540d7bb352a15` in commit
`b58c797af3861187a35aef73bda3031406e95ba2`. A second clean snapshot of that exact final SHA
returned `PASS` for all seven adapters, including `QC.LOCALIZATION.CATALOG`.

## Decision

**PARTIAL — the localization blocker and stale QC pin are resolved for the static half, but the
multi-target pilot cannot advance to integration or promotion yet.**
Runtime, extension lifecycle, deliberate negative controls, dry-run/apply/repeat/rollback,
local/GitHub parity, and pre-PR review remain unverified. The static adapter PASS does not imply
consumer or release readiness.

Runtime execution, extension lifecycle, VoiceOver, launch, controls, idempotence/rollback,
local/GitHub parity, and pre-PR receipt remain unverified. No stable promotion or release claim is
allowed from this receipt.

## Invalidation

Re-run the static probe after the German resource contract, active/vault package contents, profile,
engine pin, or source HEAD changes. A future PASS does not close the pilot until the remaining
phase-8 matrix rows are explicitly evidenced.

Next permitted action: obtain the separate authorization for the permitted runtime/matrix rows,
then execute them against the exact final consumer/QC identities. Do not infer runtime or release
readiness from this static PASS.
