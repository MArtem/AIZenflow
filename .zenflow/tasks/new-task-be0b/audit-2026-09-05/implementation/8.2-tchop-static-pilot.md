# Implementation block 8.2 — Tchop multi-target consumer pilot (partial)

Date: 2026-09-09
Model: GPT-5.6 Luna, reasoning xhigh  
Mode: эконом  
Consumer: `/Users/Artem/.zenflow/worktrees/new-task-be0b`  
Consumer HEAD: `e24b7c8be50aad8777c47116b8ccb1e4ab3a9977`
QualityControl engine: `b197bd5e8983b5c7cfd1d277dd2540d7bb352a15`
QC executable CDHash: `84ba17cfe7fcc7fd3ea7d551b07bbaba4f6dd555`

## Contract and scope

This block evaluates the existing multi-target profile and the consumer-facing quality-control
adoption path. The profile covers `TchopApp`, share/widget extension source,
`PackagesInUse`, and the declared app schemes. The final runtime block was explicitly authorized
by the app owner. All build output, simulator state, logs, receipts, and synthetic fixtures were
kept under `.zenflow/tasks/new-task-be0b/runtime/tchop-8-2/`.

The row-by-row machine-readable index is `8.2-runtime-matrix-receipt.json` in this implementation
directory. It is the authoritative compact mapping of PASS, BLOCKED, NOT_RUN_BY_USER_DECISION, and
OPEN states for this block.

The full phase-8 matrix remains mandatory: identity/static, runtime, positive and deliberate
negative controls, dry-run/apply/repeat/rollback, local/GitHub parity, and pre-PR review. Missing,
blocked, or unavailable rows keep this pilot partial.

## Static and identity evidence

- The seven deterministic consumer adapters passed on the final clean source snapshot:
  `QC.SECRETS.TRACKED`, `QC.TODO.OWNER`, `QC.GENERATED.OWNERSHIP`,
  `QC.DEPENDENCY.LOCK_DRIFT`, `QC.RESOURCES.ASSETS`, `QC.PRIVACY.MANIFEST`, and
  `QC.LOCALIZATION.CATALOG`.
- The Tchop profile and manual workflow are pinned to QC revision
  `b197bd5e8983b5c7cfd1d277dd2540d7bb352a15`.
- The pinned QC engine built successfully in a task-local SwiftPM scratch/cache. The executable
  identity is recorded above.
- `quality static` with explicit source paths passed for the final source boundary and scanned
  384 regular files. This is a valid explicit-scope static PASS, but it does not assert Xcode
  source membership.
- Positive and deliberate-negative QC controls both behaved as designed. The safe fixture returned
  `PASS`; the `.canary-fail` fixture returned `FAIL` with `QC.STATIC.FORBIDDEN_ARTIFACT` and exit
  code 1. Receipts: `runtime/tchop-8-2/qc-logs/static-fixture-pass.json` and
  `runtime/tchop-8-2/qc-logs/static-fixture-fail.json`.
- The full bootstrap lifecycle on a synthetic isolated project passed: `inventory`, `dry-run`,
  authorized `apply`, `post-check`, repeat apply with no new writes, and authorized `rollback`.
  Rollback removed only the created file/directories; the journal container was preserved. Evidence
  is under `runtime/tchop-8-2/bootstrap-fixture/`.

## App-local remediation — German locale

The app owner selected Option A from `8.2-german-locale-contract.md`: German remains unsupported
until a complete reviewed catalog exists. The incomplete `de.lproj` was removed from both active
and vault product-resource copies in consumer commit `c0e8ec593c6411eb47e3f5075cf90c23200a5d4a4`.
No English-to-German copying, machine translation, QC weakening, or reusable `AppLocalization`
change was made.

## Runtime and build evidence

- The direct Debug build passed for all six declared schemes on iPhone 17 Pro, iOS 26.5,
  Xcode 26.6: `TchopApp`, `TchopAppOcean`, `TchopShareExtension`, `TchopWidgetsExtension`,
  `TchopShareOceanExtension`, and `TchopWidgetsOceanExtension`. Logs are the corresponding
  `*-debug-sequential-final.log` files under `runtime/tchop-8-2/`.
- A clean signed-out installation/launch passed and produced `screenshots/signed-out-clean.png`
  showing the login screen.
- An authenticated launch using the test-only simulator session environment passed after fixing
  the asynchronous session-restore race; `screenshots/authenticated-after-session-fix.png` shows
  the shell/feed with the tab bar and empty-state UI. The earlier login screenshot is retained as
  pre-fix diagnostic evidence and is not counted as a PASS.
- The runtime block found and corrected three concrete defects: stale German Xcode project
  references, Swift 6 overload ambiguity in `APIOfflineQueue`, and `AppState` clearing an explicit
  test session during asynchronous restore. These fixes are in consumer commit
  `e24b7c8be50aad8777c47116b8ccb1e4ab3a9977`.

## Blocked or intentionally unclaimed rows

- `quality static-evidence` is blocked by `QC.PROFILE.XCODE_GRAPH_RESOLUTION_REQUIRED`:
  schemaVersion 2 requires authoritative Xcode graph resolution, and the engine refuses to infer
  that claim from a direct build. This is an engine/profile evidence boundary, not a static PASS.
- `quality build-evidence` is blocked by `QC.BUILD_EVIDENCE.PREFLIGHT_UNTRUSTED`; no authenticated
  build-evidence receipt was produced.
- Existing `TchopAppTests` and `TchopAppUITests` did not execute because the scheme build fails on
  the pre-existing test double `TestFeedCardRepository`, which no longer conforms to
  `FeedCardPersisting` after the production protocol requires `userID` parameters. Test source was
  not modified in this block. Logs: `tchopapp-test-all.log` and `tchopapp-ui-tests.log`.
- Extension installation/activation flows, VoiceOver traversal, and full accessibility interaction
  were not run. The extension schemes were compiled, but compile success is not lifecycle evidence.
- Local/GitHub parity was not run because GitHub execution remains manual-only by profile and no
  workflow dispatch was authorized. This receipt therefore does not claim CI parity.
- Archive, signing, privacy, performance/Instruments, App Store, and release evidence remain
  outside this pilot scope.

## Decision

**PARTIAL — static identity, direct multi-target builds, app smoke launch, negative controls, and
reversible bootstrap are evidenced; the pilot cannot advance to integration or promotion.** The
Xcode graph evidence boundary, test-target compilation blocker, extension lifecycle/accessibility,
local/GitHub parity, and pre-PR independent review remain open. No stable QC promotion, production
readiness, or release claim is allowed from this receipt.

## Invalidation and next action

Re-run the matrix after the source HEAD, profile, engine pin, Xcode project graph, test double,
workflow authorization, or extension lifecycle changes. The receipt is invalidated by any new
commit unless its exact HEAD and evidence are re-recorded. Next implementation steps are to repair
the test-double contract in a separately authorized test-source block, add an authenticated Xcode
graph/build-evidence path that the engine accepts, and then repeat extension/accessibility and
local/GitHub parity checks. Until those rows pass or receive an explicit bounded exception, 9.1 and
9.2 remain blocked.

**перечитать весь актуальный набор документации и правил для этого worktree и task-контекста**
