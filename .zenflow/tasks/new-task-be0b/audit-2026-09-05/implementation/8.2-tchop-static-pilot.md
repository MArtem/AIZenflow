# Implementation block 8.2 — Tchop multi-target consumer pilot (partial)

Date: 2026-09-09
Model: GPT-5.6 Luna, reasoning xhigh  
Mode: эконом  
Consumer: `/Users/Artem/.zenflow/worktrees/new-task-be0b`  
Consumer HEAD for current exact-SHA evidence: `87d6ce2edd1ce59097888852fc3925ae527d51b3`
Implementation commit: `7f6277ab5783dc764c60af339491991100119caa`
Profile-boundary evidence commit: `87d6ce2edd1ce59097888852fc3925ae527d51b3`
Working tree at exact-SHA evidence: clean for tracked files after bounded share-host fixture and evidence commits; QC-generated cache/runtime remains local and ignored
QualityControl engine: `bc2072b76df41a653f204861d60d9d602ac999af`
QC executable CDHash: `6a41e4aae31edded3342fe2ecc7f043383948e40`

## Contract and scope

This block evaluates the existing multi-target profile and the consumer-facing quality-control
adoption path. The profile covers `TchopApp`, share/widget extension source,
`PackagesInUse`, and the declared app schemes. The final runtime block was explicitly authorized
by the app owner. All build output, simulator state, logs, receipts, and synthetic fixtures were
kept under `.zenflow/tasks/new-task-be0b/runtime/tchop-8-2/`.

The row-by-row machine-readable index is `8.2-runtime-matrix-receipt.json` in this implementation
directory. It is the authoritative compact mapping of PASS, BLOCKED, NOT_RUN_BY_USER_DECISION, and
OPEN states for this block.

The full phase-8 matrix remains mandatory: identity/static, runtime, positive and deliberate
negative controls, dry-run/apply/repeat/rollback, local/GitHub parity, and pre-PR review. Missing,
blocked, or unavailable rows keep this pilot partial.

## Static and identity evidence

- The seven deterministic consumer adapters passed on the final clean source snapshot:
  `QC.SECRETS.TRACKED`, `QC.TODO.OWNER`, `QC.GENERATED.OWNERSHIP`,
  `QC.DEPENDENCY.LOCK_DRIFT`, `QC.RESOURCES.ASSETS`, `QC.PRIVACY.MANIFEST`, and
  `QC.LOCALIZATION.CATALOG`.
- The Tchop profile and manual workflow are pinned to QC revision
  `b197bd5e8983b5c7cfd1d277dd2540d7bb352a15`.
- The pinned QC engine built successfully in a task-local SwiftPM scratch/cache. The executable
  identity is recorded above.
- `validate-profile` passed the structural schema-v2 contract on the final pinned engine. The
  graph-scoped static gate remains conservative and still requires Xcode graph evidence.
- `quality static` with explicit source paths passed for the final source boundary and scanned
  384 regular files. This is a valid explicit-scope static PASS, but it does not assert Xcode
  source membership.
- Positive and deliberate-negative QC controls both behaved as designed. The safe fixture returned
  `PASS`; the `.canary-fail` fixture returned `FAIL` with `QC.STATIC.FORBIDDEN_ARTIFACT` and exit
  code 1. Receipts: `runtime/tchop-8-2/qc-logs/static-fixture-pass.json` and
  `runtime/tchop-8-2/qc-logs/static-fixture-fail.json`.
- The full bootstrap lifecycle on a synthetic isolated project passed: `inventory`, `dry-run`,
  authorized `apply`, `post-check`, repeat apply with no new writes, and authorized `rollback`.
  Rollback removed only the created file/directories; the journal container was preserved. Evidence
  is under `runtime/tchop-8-2/bootstrap-fixture/`.

## Bounded Share Extension host fixture

The consumer now contains a standalone test-host fixture at
`TchopAppUITests/ShareHostFixture/TchopShareHostFixture.xcodeproj`. It presents a deterministic
`UIActivityViewController` action sheet and uses explicit accessibility identifiers for the host
button and Share Extension screen/close control. The fixture project, plist, and modified Share
Extension/UI-test sources passed Swift parse, plist, xcodeproj, diff, and Debug build checks. The
fixture and `TchopApp` bundles were installed with `simctl` on the restored iPhone 17 Pro
Simulator.

The opt-in targeted test is enabled only with `TCHOP_SHARE_HOST_FIXTURE=1`. Two attempts reached
Xcode test orchestration but exited 70 before test execution because CoreSimulatorService made the
known booted destination unavailable to `xcodebuild`; the simulator service reported connection
invalid/unavailable and no Share activation or VoiceOver traversal PASS is claimed.

## App-local remediation — German locale

The app owner selected Option A from `8.2-german-locale-contract.md`: German remains unsupported
until a complete reviewed catalog exists. The incomplete `de.lproj` was removed from both active
and vault product-resource copies in consumer commit `c0e8ec593c6411eb47e3f5075cf90c23200a5d4a4`.
No English-to-German copying, machine translation, QC weakening, or reusable `AppLocalization`
change was made.

## Runtime and build evidence

- The direct Debug build passed for all six declared schemes on iPhone 17 Pro, iOS 26.5,
  Xcode 26.6: `TchopApp`, `TchopAppOcean`, `TchopShareExtension`, `TchopWidgetsExtension`,
  `TchopShareOceanExtension`, and `TchopWidgetsOceanExtension`. Logs are the corresponding
  `*-debug-sequential-final.log` files under `runtime/tchop-8-2/`.
- A clean signed-out installation/launch passed and produced `screenshots/signed-out-clean.png`
  showing the login screen.
- An authenticated launch using the test-only simulator session environment passed after fixing
  the asynchronous session-restore race; `screenshots/authenticated-after-session-fix.png` shows
  the shell/feed with the tab bar and empty-state UI. The earlier login screenshot is retained as
  pre-fix diagnostic evidence and is not counted as a PASS.
- The runtime block found and corrected three concrete defects: stale German Xcode project
  references, Swift 6 overload ambiguity in `APIOfflineQueue`, and `AppState` clearing an explicit
  test session during asynchronous restore. These fixes are in consumer commit
  `e24b7c8be50aad8777c47116b8ccb1e4ab3a9977`.

## Authorized test-source repair — 2026-09-09

The app owner separately authorized bounded repairs of the existing test source. Only these three
test files changed:

- `TchopAppTests/TestDoubles/TestAppContentRepository.swift`
- `TchopAppTests/NewsFeedViewModelTests.swift`
- `TchopAppUITests/TchopAppUITests.swift`

The repository double now conforms to the user-scoped `FeedCardPersisting` contract and validates
the deterministic `test-user` used by the affected store tests. Direct repository tests now pass
the required user identifier, and direct `FeedCardStore` setups activate that user before
operations. `git diff --check`, a stale-signature search, and parse-only Swift syntax validation
for both changed files pass. All four direct `TestFeedCardRepository`/`FeedCardStore` setups
activate `test-user`; no other test-double creation sites remain.

Runtime verification now passes for the repaired test scope: `TchopAppTests` passed with
`** TEST SUCCEEDED **`; the UI-test `build-for-testing` passed; and all seven UI tests passed with
zero failures. The UI helper now follows the product flow by selecting `New post` after the shell
`Create` action sheet opens. The container identifier itself is not surfaced in the accessibility
tree, so the test synchronizes on the localized action button instead.

## Follow-up boundary probes — 2026-09-09

- A clean committed-HEAD probe kept the profile and engine checkouts isolated from the dirty
  working tree. The pre-patch observation recorded two independent contract mismatches:
  `TchopAppTests`/`TchopAppUITests` were declared under the `TchopApp` scheme although the
  selected build-settings inventory exposed only `TchopApp`, and the profile pinned
  `git-b197bd5` while the engine reported `0.1.0-dev`. Evidence:
  `runtime/tchop-8-2/qc-logs/graph-and-extension-probe-2026-09-09.json`.
- An app-local QC profile patch now aligns the engine version with the built executable
  (`0.1.0-dev`), declares only the observed `TchopApp` build target, and places the QC cache under
  the declared `TchopApp` source boundary so generated Xcode inputs are not mistaken for unscoped
  product source. `validate-profile` still reports
  `QC.PROFILE.XCODE_GRAPH_RESOLUTION_REQUIRED` because the engine's schemaVersion 2 gate remains
  authoritative. The new exact-SHA supervised build now passes compiler source-membership
  verification; receipt: `runtime/tchop-8-2/qc-logs/exact-sha-profile-boundary-build-pass-2026-09-09.json`.
- The already-built app bundle was installed on the iPhone 17 Pro Simulator and its embedded
  `TchopShareExtension.appex` and `TchopWidgetsExtension.appex` were present. Direct Share
  activation still requires a supported host application's share-sheet interaction, and VoiceOver
  requires native accessibility traversal. A targeted terminal-only XCUITest probe reached Safari
  but found no accessibility `Share` button, so it could not open the share sheet or activate
  `Tchop Share`; receipt: `runtime/tchop-8-2/qc-logs/extension-host-probe-failure-2026-09-09.json`.
  The lifecycle/accessibility row remains `BLOCKED`, not PASS.

## Blocked or intentionally unclaimed rows

- `quality static-evidence` has no authenticated PASS: the final exact-SHA rerun authenticates the
  checkout but returns `QC.PROFILE.XCODE_GRAPH_RESOLUTION_REQUIRED` with verification verdict
  `BLOCKED`; structural `validate-profile` passes, while graph-scoped validation remains blocked.
- `quality build-evidence` now has an authenticated PASS for the exact profile-boundary commit:
  `QC.BUILD`, `QC.BUILD.MEMBERSHIP`, `QC.BUILD.FIRST_PARTY_WARNINGS`, and
  `QC.CONCURRENCY.DIAGNOSTICS` all pass with verification verdict `READY`.
- `TchopAppTests` passed, `TchopAppUITests` built successfully, and all 7 UI tests passed with 0
  failures. Evidence: `runtime/tchop-8-2/test-repair/`.
- Extension package inspection, host fixture build, app build, and app installation passed, but
  Share activation, VoiceOver traversal, and full accessibility interaction remain BLOCKED because
  the targeted XCUITest could not acquire the Simulator destination. Extension compilation and
  package presence are not lifecycle evidence.
- Bounded `quality doctor` passed profile/repository/project/source/sandbox checks but remains
  BLOCKED on `QC.DOCTOR.XCODE_EFFECTIVE_SETTINGS`; the local Xcode/CoreSimulator service could not
  provide effective settings within the bounded query budget.
- Local/GitHub parity was not run because GitHub execution remains manual-only by profile and no
  workflow dispatch was authorized. This receipt therefore does not claim CI parity.
- Archive, signing, privacy, performance/Instruments, App Store, and release evidence remain
  outside this pilot scope.

## Decision

**PARTIAL — static identity, structural profile validation, explicit static scan, supervised
exact-SHA build, direct multi-target builds, app smoke launch, negative controls, reversible
bootstrap, and the bounded host fixture build are evidenced; the pilot cannot advance to
integration or promotion.** The graph-scoped schema-v2 static-evidence gate, bounded doctor
effective-settings check, extension lifecycle/accessibility runtime, local/GitHub parity, and
pre-PR independent review remain open. No stable QC promotion, production readiness, or release
claim is allowed from this receipt.

## Invalidation and next action

Re-run the matrix after the source HEAD, profile, engine pin, Xcode project graph, test double,
workflow authorization, Simulator service recovery, or extension lifecycle changes. The receipt
is invalidated by any new commit unless its exact HEAD and evidence are re-recorded. Unit/UI
verification and the supervised exact-SHA build now pass; the bounded host fixture is compiled and
installed but its opt-in UI execution is blocked by CoreSimulatorService. The next required
boundaries are authoritative graph-scoped schema-v2 acceptance, doctor effective-settings
availability, extension host/VoiceOver execution, and manual GitHub parity. Local/GitHub parity
remains manual-only.
Until those rows pass or receive an explicit bounded exception, 9.1 and 9.2 remain blocked.

**перечитать весь актуальный набор документации и правил для этого worktree и task-контекста**
