# Implementation receipt — 5.3 catalog maturity and honest mode coverage

Date: 2026-09-08
Model: GPT-5.6 Luna, reasoning xhigh
Mode: эконом
Owner: QualityControl policy catalog, adapter boundary, and task evidence

## Contract

| Dimension | Decision |
| --- | --- |
| Behavior | Every catalog entry reports `implemented`, `verified`, `wired`, `pilotEnabled`, and exact `evidence` independently. |
| Authority | `implementation` remains the policy declaration; maturity fields describe observed engine and promotion state. A direct adapter command cannot assert mode coverage or pilot readiness. |
| Producer/consumer | `policies/check-catalog.json` + schema → `catalog_entry` validator for direct adapters; trusted Swift mode paths remain the only `wired=true` entries until other adapters are integrated. |
| State/time | Maturity describes the committed QC revision and bounded evidence available at this block; it does not silently inherit future test, build, or pilot results. |
| Input/resource envelope | Maturity flags are bounded booleans; evidence is a bounded list of at most 16 non-empty repository-relative strings. |
| Failure semantics | Missing/malformed maturity, verified-without-evidence, or pilot-enabled-without-verified-and-wired state is `BLOCKED` for direct adapters or schema-invalid for catalog transport. |
| Affected claims | 20 catalog IDs are now auditable without collapsing implementation, verification, trusted mode wiring, and consumer-pilot promotion into one label. |

## Implemented changes

- Bumped catalog version to `1.1.0` and added required maturity fields to all 20 entries.
- Marked only the four currently trusted engine paths as `wired`: profile contract, static source
  boundary, forbidden-artifact scan, and build membership. Direct Python adapters remain explicitly
  unwired until a mode dispatcher invokes them.
- Kept `pilotEnabled=false` for every gate because canary and consumer-pilot promotion have not run.
- Marked only the three Swift deterministic entries verified after clean temporary Git fixture
  copies; each lists exact positive/negative fixture paths and the fixture contract README. Other
  entries remain honest pending even when their adapter source exists.
- Extended `check-catalog.schema.json` and `catalog_entry` so maturity cannot be omitted or silently
  weakened by a direct adapter invocation.
- Documented the maturity semantics in QC README, policies README, and schemas README.

## Acceptance mapping

- All 20 catalog entries have the required five-field maturity object.
- `implemented` agrees with the legacy `implementation` value for every entry.
- `verified=true` entries have non-empty evidence references; staged/review-candidate entries remain
  unimplemented and unwired.
- No `pilotEnabled` entry exists before the later canary and two-consumer promotion gates.
- Direct adapter invocation is explicitly documented as adapter evidence, not mode coverage.
- A changed maturity object cannot pass the adapter catalog boundary if its structure or consistency
  invariants are violated.

## Verification

- JSON parse and Python AST parse: PASS.
- Catalog invariants: PASS for 20 entries; no pilot-enabled entry; all verified entries have evidence.
- Clean QC `HEAD` `d75a0d590836edb4dc0ed29ab9ccc5ad0ce9717b`: `QC.STATIC.SWIFT_HOT_PATH` PASS and
  `QC.STATIC.SWIFT_CONCURRENCY_ESCAPE` PASS after the new catalog validator.
- Existing Swift hot-path, concurrency-escape, and disabled-test positive/negative fixtures were
  copied to a private task-runtime Git root, committed, and invoked through the bounded adapter:
  all six expected exits matched (PASS/PASS/PASS for positive controls; FAIL/FAIL/FAIL for negative
  controls). The temporary root is not a product repository or durable fixture mutation.
- `git diff --check`: PASS before commit; QC worktree clean after commit/push.
- No test source, XCTest/Swift Testing runner, Xcode build, Simulator/device, Instruments, signing,
  workflow, or pilot action was run.

## Commit and remote receipt

- QualityControl branch: `main`
- Commit: `d75a0d590836edb4dc0ed29ab9ccc5ad0ce9717b`
- Remote: `origin/main` resolves to the same SHA after push.
- Worktree: clean after commit/push.

## Residual risk and rollback

The catalog does not yet prove that every implemented adapter is consumed by a trusted mode, nor that
any gate is safe for a consumer project. Stage 7.1 must exercise the new maturity and adapter claims;
stage 8 must promote only gates with required consumer evidence. Rollback is the prior QC pin
`0266873b68948b596903388eba28a125fcd8990e`; do not restore a single overloaded implementation flag.

**перечитать весь актуальный набор документации и правил для этого worktree и task-контекста**
