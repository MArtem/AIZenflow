# Implementation receipt — 4.3 neutral new-project scenario

Date: 2026-09-08
Model: GPT-5.6 Luna, reasoning xhigh
Mode: эконом
Owner: task evidence; disposable consumer is local-only

## Scope and authority

This block exercised the new-project contract in the ignored local consumer
`/Users/Artem/.zenflow/worktrees/new-task-be0b/.zenflow/tasks/new-task-be0b/runtime/disposable-consumer-4-3`.
It is an educational static scenario, not a product app, pilot, engine adoption, or reusable
policy source. No canonical app boundary or Documentation Vault reusable rule was changed by the
consumer itself.

## Scenario path

1. Created a clean local Git root from the canonical reusable baseline.
2. Installed the bootstrap marker, portable snapshot, routed baseline, secret-ignore template,
   task plan/handoff, and a minimal project profile.
3. Recorded quality-control adoption as `DEFERRED` with an owner and revisit condition because the
   consumer has no Xcode target, runtime, or approved pilot.
4. Added the neutral S01 request-ordering exercise: a value-type state owner accepts a result only
   when its request identity is current.
5. Applied a small change that models cancellation separately from failure.
6. Ran the thin local launcher, resolved the selected routes, reviewed the exact Git diff, and
   transferred continuation through the disposable task handoff.

The initial attempt exposed two real routing issues: project-local docs placed at active top level
were unclassified, and the baseline README lacked a governance link required by the bootstrap
contract. Moving local docs under `docs/project/` and adding the local link fixed the consumer
without changing reusable authority.

## Evidence

- Bootstrap commit: `78853b00bf57450c3c732e48b7dfc9185858f2a1`.
- Exercise commit: `85be33da19db46fa8c4741bb91ad169bbe179488`.
- Cancellation change commit: `7223c922acddc84486dfa65233f357bcbf3a726b`.
- Continuation/handoff HEAD: `9d920d1834ba588b1b06b3b871df8848f9a74220`.
- Consumer inventory: 188 files excluding `.git`; deterministic inventory SHA-256
  `cd9f8674bdb7eaa651ef0b55e7f7590e1f36d825570f312f0e5d73e64fed1620`.
- The disposable Git root is clean at the recorded HEAD.
- Selected route: `Level 0 + ios-new-app-bootstrap + quality-control-governance`.

## Verification

- `scripts/check_bootstrap_contract.py`: PASS.
- `scripts/check_task_type_documentation_router.py`: PASS, 90 documents, Level 0 2862/5000 words.
- `scripts/check_docs_consistency.py`: PASS.
- `scripts/check_documentation_boundaries.py`: PASS.
- `scripts/quality_check.sh`: PASS.
- `scripts/resolve_docs_route.py --task-id disposable-4-3 ios-new-app-bootstrap quality-control-governance`: PASS; no missing, unclassified, duplicate, or unknown route findings.
- Exact diff from bootstrap commit and `git diff --check`: PASS.
- No build, tests, Simulator, device, Instruments, signing, archive, CI execution, network,
  backend, payment, account, product backlog, or release evidence was claimed.

## Acceptance and rollback

The 4.3 acceptance is met for this static scenario: bootstrap applied without a reminder, project
facts remained local, the educational architecture did not become a product default, missing
evidence stayed explicit, and the handoff restored the next action in a subsequent continuation
pass. The consumer is not a stable QC pilot and does not satisfy the later two-pilot promotion
gate.

Rollback is deletion of the ignored disposable runtime directory or retention for provenance; no
tracked user path, app repository, or canonical reusable source needs rollback.

## Residual limits

The scenario proves documentation/bootstrap routing and a static state contract only. It does not
prove Swift compilation, runtime ordering, cancellation behavior, UI, persistence, performance,
release readiness, or engine accuracy. Those require their explicitly gated later phases.

**перечитать весь актуальный набор документации и правил для этого worktree и task-контекста**
