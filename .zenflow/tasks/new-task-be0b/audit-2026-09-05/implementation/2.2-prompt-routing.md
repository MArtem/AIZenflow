# Implementation receipt — block 2.2

Date: 2026-09-08
Model: GPT-5.6 Luna, xhigh
Mode: economy
Scope: normalize reusable active prompts and specialist routes; no engine, app source, tests,
runtime, CI, hooks, rollout, or paid services.

## Change contract

- AI work enters through stable route IDs and reads only the relevant numbered ranges of the
  master prompt; the full master remains a reference for broad/cross-cutting work or master
  maintenance.
- Prompt presets preserve project authority and select architecture, state ownership, design
  tokens, verification, previews, fakes, and rollout mechanisms from the current project profile
  and permission scope instead of imposing a universal stack.
- Overlapping API, transport, offline-sync, and testing routes have one primary specialist owner;
  narrow supplements are added only for an additional risk actually in scope.
- Local Swift runtime/concurrency skills carry explicit version, review date, and provenance;
  upstream text is reference data and cannot silently replace the governed local skill.
- Test-generation prompts remain test-only and retain meaningful behavior coverage, while ordinary
  feature/review/design/refactoring prompts do not create or require tests by default.

## Implemented changes

- `AI_iOS_TASK_ROUTER.md`: stable route IDs `AI.CLASSIFY` through `AI.DELIVERY`, section-range
  locators, specialist ownership matrix, and no generic keyword activation.
- `AI_iOS_MASTER_PROMPT.md`: routed loading contract, compact plan/report for bounded work,
  conditional mock/fake seams, and explicit full-master exception.
- Feature, ADR, refactoring, CI/debug, SwiftUI design, and test prompt presets: existing project
  architecture/profile, existing design/localization tokens, conditional render-ready state and
  component extraction, explicit intents or approved reducer action, and permission-bounded
  verification.
- `ios-api-contracts`, `ios-network-resilience`, `ios-offline-sync`, and `ios-test-strategy`:
  action/risk-specific triggers; `ios-swift-runtime` and `ios-concurrency-runtime`: local
  version/provenance metadata.

## Acceptance scenarios

1. A small UI/SwiftUI fix can load only the selected prompt/router ranges and does not import the
   complete AI master or an unrelated architecture stack.
2. Direct use of `feature-specific-quick` does not demand tests, action enums, mocks, previews,
   flags, or rollback unless the approved scope and current policy require them.
3. API schema/mapping, transport timeout/retry, and durable replay/conflict select distinct primary
   skills with only narrow supplements for combined risk.
4. Figma/SwiftUI design work follows the current model-routing and verification permissions and
   uses existing project tokens rather than a mandatory AppTheme/MVVM stack.
5. Swift/concurrency skills expose governed local provenance and review metadata; upstream content
   cannot become local authority implicitly.

## Evidence

- Canonical Documentation Vault commit: `b7a975b395e90937c38f86aa43c27a19c1108d29`, pushed to
  `origin/main` and verified with `ls-remote`.
- `scripts/check_documentation_vault.py`: PASS (`3732 files; 4 app boundaries`).
- `check_task_type_documentation_router.py`: PASS (`87 documents classified; Level 0 2823/5000
  words`).
- `check_docs_consistency.py`: PASS.
- `check_documentation_boundaries.py`: PASS.
- `git diff --check`: PASS before commit.

No build, test, simulator, Instruments, CI, or runtime evidence was created; these are outside
this documentation-only block and remain permission-bound.

## Rollback

Revert the canonical commit and its task receipt, then restore the prior prompt/routing files and
remote SHA. Do not delete historical ZIP/archive material; it remains non-authoritative reference
data.
