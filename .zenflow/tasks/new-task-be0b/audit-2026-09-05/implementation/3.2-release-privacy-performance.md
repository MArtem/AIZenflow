# Implementation receipt — 3.2 release/privacy/performance matrices

Date: 2026-09-08  
Model: GPT-5.6 Luna, reasoning xhigh  
Mode: эконом  
Owner: Documentation Vault (reusable contract); consuming projects own row values and evidence

## Scope and authority

This block implemented a reusable project-owned matrix contract. The canonical authority is
Documentation Vault commit `d88f1289772d5ed2abe06a8e44dd0d38f4d75208` on `origin/main`.
No app source, build setting, deployment target, engine, package source, test, runtime, CI,
submission, cloud provider or paid service was added or changed.

## Contract delivered

- Added `reusable/baseline/docs/IOS_RELEASE_PRIVACY_PERFORMANCE_MATRIX.md` with one-row fields
  for axis/claim, scope, profile, owner/authority, applicability, budget/requirement, evidence
  route/state, fallback/rollback and verdict.
- Kept App Store Connect upload SDK/Xcode floor separate from each target's minimum deployment OS;
  the current Apple requirement is recorded by source URL/date at release time rather than copied as
  a permanent number into the reusable baseline.
- Connected actual privacy manifest/API/SDK behavior to permission, retention, logout, deletion,
  export and ownership rows; structural manifest validity is explicitly not App Review compliance.
- Added scenario-level launch, interaction, frame/hitch, main-thread and memory evidence fields;
  the 250 ms diagnostic hang signal is not used as a responsiveness budget.
- Added iPhone/iPad/window, VoiceOver, Dynamic Type, RTL, keyboard/pointer, contrast and Reduce
  Motion matrix dimensions; screenshots remain supporting evidence only.
- Put Core AI/Foundation Models and other new capabilities on a separate availability track with
  device/region/model readiness, data boundary, fallback, flag/rollback and evidence fields; no
  cloud processing or infrastructure is implied.
- Routed the standard through release, security/privacy, accessibility/QA, performance and AI
  routes, added `QC.RELEASE.PROFILE`, and extended the compatibility matrix and specialist docs.

## Verification

- Canonical manifest regenerated with `python3 -B scripts/generate_manifest.py --write` and then
  verified with `--check`.
- Router check: PASS (`90 documents classified; Level 0 2862/5000 words`).
- Documentation consistency check: PASS.
- Documentation boundary check: PASS (`/Users/Artem/.zenflow/worktrees/documentation-vault`).
- JSON parse checks for routing registry and task routes: PASS.
- `git diff --check`: PASS before commit.
- Remote receipt: local `HEAD` and `origin/main` both equal
  `d88f1289772d5ed2abe06a8e44dd0d38f4d75208`.
- No build, test, Simulator, device, Instruments, CI, signing, App Store submission or runtime
  evidence was collected; those remain permission-bound evidence phases.

## Acceptance and rollback

Roadmap acceptance is met: upload floor and deployment target are distinct; privacy rows connect
manifest/API/SDK/data use and lifecycle; budgets are measurable and scenario-specific; platform and
accessibility dimensions are profile-owned; structural manifest PASS, screenshots and 250 ms hang
signals are not overstated; no automatic cloud or infrastructure is introduced. Rollback is a
documentation revision/pin rollback, without changing app capabilities or build settings.

## Residual limits

The matrix defines fields and evidence semantics. It does not prove a consuming project's App
Review, privacy, accessibility, performance, release or Foundation Models readiness until its
project-owned rows contain permitted current evidence.

**перечитать весь актуальный набор документации и правил для этого worktree и task-контекста**
