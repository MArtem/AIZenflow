# Implementation block 10.1 — context budget and evidence reuse

Date: 2026-09-08  
Model: GPT-5.6 Luna, reasoning xhigh  
Mode: эконом  
Task HEAD measured: `657a150e5b55193c95ceb61b45a69ec66bb8ecfb`

## Measurement contract

The report measures words and bytes in transitive documentation routes. These are planning
proxies, not billed token telemetry. Required Level 0, repository instructions, task state,
permissions, release/data/concurrency risk routes, and evidence identity bindings may not be
removed to claim savings.

## Current measurements

Source: `audit-2026-09-05/context-cost.json` baseline plus a fresh read-only report from the current
router/task state.

| Envelope | Documents | Words | Bytes | Budget/result |
| --- | ---: | ---: | ---: | --- |
| Level 0 | 3 | 2,852 | 24,624 | below 5,000 |
| Dynamic `plan.md` + `handoff.md` | 2 | 1,922 | 23,529 | below 3,500 |
| Instruction envelope | 6 | 6,399 | 60,466 | no configured overflow |
| `ios-concurrency-runtime` route only | 3 | 1,606 | — | targeted route |
| `ios-universal-quality` route only | 7 | 7,810 | — | broad route |

The targeted concurrency route is approximately 79% smaller than the broad universal-quality
route by route-only words. This is a routing comparison, not a before/after optimization claim and
not a price guarantee.

## Coverage checks and sentinel routing

- `check_task_type_documentation_router.py`: PASS; 94 documents classified.
- `check_docs_consistency.py`: PASS.
- Current report: zero missing, unclassified, or resolver failures; all configured budgets remain
  within bounds.
- Sentinel policy: a documentation rename uses documentation/task-state routes; a concurrency or
  lifetime change uses `ios-concurrency-runtime` plus the applicable governance route; a release,
  privacy, or data-loss change selects its specialist route instead of the broad universal route;
  an exception change uses local-exception and governance routes. The route registry preserves the
  required authority/permission documents for each class.

## Decision

**PASS_WITH_LIMITATION for the bounded context-budget contract.** The current router already
provides compact targeted routes, keeps Level 0 and active task state below their budgets, and
supports read-once/re-read-on-change evidence reuse. No mandatory document or response-contract
text was removed. A 30–50% billed-token reduction is not claimed because no comparable billed-token
telemetry exists; future measurements must compare the same sentinel route and source identity.

## Rollback and invalidation

No router or reusable rule changed in this block, so rollback is the prior measurement receipt.
This receipt becomes stale when Level 0, task-state shape, route registry, AGENTS, or specialist
route sources change. Any future optimization that drops coverage must be reverted at the specific
route rather than compensated by rereading the full archive.

Next block: 10.2 Luna xhigh process calibration using the observations and representative receipts.
