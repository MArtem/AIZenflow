// swift-tools-version: 6.0

import PackageDescription

let package = Package(
    name: "{{HelperName}}",
    platforms: [
        .iOS(.v17),
        .macOS(.v14),
        .tvOS(.v17),
        .watchOS(.v10),
        .visionOS(.v1)
    ],
    products: [
        .library(
            name: "{{HelperName}}",
            targets: ["{{HelperName}}"]
        )
    ],
    dependencies: [
        // Helper packages may depend on the packages they integrate.
        // Prefer local path dependencies only inside IntegrationHelpers, never inside root packages.
        // Example: add a local or remote package dependency here when this helper is used.
        // Keep root packages dependency-free; helper packages are the only allowed composition layer.
    ],
    targets: [
        .target(
            name: "{{HelperName}}",
            dependencies: [
                // .product(name: "AppPackageA", package: "AppPackageA"),
                // .product(name: "AppPackageB", package: "AppPackageB")
            ],
            path: "Sources/{{HelperName}}"
        ),
        .testTarget(
            name: "{{HelperName}}Tests",
            dependencies: ["{{HelperName}}"],
            path: "Tests/{{HelperName}}Tests"
        )
    ]
)
