# {{PackageName}} Contract

## Standalone guarantee

`{{PackageName}}` is intended to be a 100% single-folder standalone Swift Package.

```text
[ ] No sibling path dependencies
[ ] No sibling package imports
[ ] No app-specific product logic
[ ] No raw sensitive telemetry
[ ] Tests included
[ ] Docs included
[ ] Local verify script included
```

## Responsibility

This package owns:

```text
- TODO
```

This package does not own:

```text
- app-specific copy;
- feature routing;
- product UI;
- cross-package integration.
```

## Public API surface

Main types:

```text
- TODO
```

## Platform support

```text
iOS: 17+
macOS: 14+
tvOS: 17+
watchOS: 10+
visionOS: 1+
```

Adjust if the package requires Apple-only frameworks.

## Concurrency contract

Document:

```text
- actor isolation;
- Sendable assumptions;
- cancellation behavior;
- thread-safety guarantees.
```

## Privacy contract

Document:

```text
- sensitive data handled;
- redaction behavior;
- telemetry emitted;
- what is forbidden.
```

## Testing contract

Package tests must cover:

```text
- happy path;
- failure path;
- edge cases;
- privacy boundaries if relevant;
- concurrency/cancellation if relevant.
```
